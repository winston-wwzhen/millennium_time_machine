<!-- 农场成就徽章组件 -->
<view class="farm-badge-container">
  <view class="section-title">🌾 我的农场</view>

  <!-- 农场数据卡片 -->
  <view class="farm-card">
    <view class="farm-header">
      <text class="farm-icon">🏡</text>
      <text class="farm-title">千禧农场</text>
    </view>

    <view class="farm-stats">
      <view class="stat-row">
        <view class="stat-item">
          <text class="stat-value">{{farmData.level}}</text>
          <text class="stat-label">等级</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-value">{{farmData.coins}}</text>
          <text class="stat-label">金币</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-value">{{farmData.exp}}</text>
          <text class="stat-label">经验</text>
        </view>
      </view>
    </view>

    <view class="farm-actions">
      <button class="action-btn" bindtap="shareAchievement">
        <text class="btn-icon">📢</text>
        <text class="btn-text">分享成就</text>
      </button>
      <button class="action-btn" bindtap="goToFarm">
        <text class="btn-icon">🚜</text>
        <text class="btn-text">进入农场</text>
      </button>
    </view>
  </view>

  <!-- 分享对话框 -->
  <view class="win98-overlay" wx:if="{{showShareDialog}}">
    <view class="win98-window dialog-box">
      <view class="win98-title-bar">
        <view class="win98-title-text">分享农场成就</view>
        <view class="win98-title-controls">
          <button class="win98-btn-close" bindtap="closeDialog"></button>
        </view>
      </view>
      <view class="win98-window-body">
        <view class="achievement-preview">
          <text class="preview-text">{{generateAchievementText()}}</text>
        </view>
        <view class="dialog-actions">
          <button class="win98-btn-std" bindtap="copyAchievement">复制文本</button>
          <button class="win98-btn-std" bindtap="shareToWeChat">分享到微信</button>
          <button class="win98-btn-std" bindtap="closeDialog">取消</button>
        </view>
      </view>
    </view>
  </view>
</view>
