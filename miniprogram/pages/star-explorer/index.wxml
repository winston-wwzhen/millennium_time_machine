<view class="win98-desktop">

<view class="window-container game-window">
  
  <view class="menu-overlay" wx:if="{{showGameMenu}}" catchtap="closeMenu"></view>

  <view class="title-bar">
    <view class="title-text">{{headerTitle}}</view>
    <view class="title-controls">
      <view class="control-box">_</view>
      <view class="control-box" bindtap="exitGame">×</view>
    </view>
  </view>

  <view class="menu-bar">
    <view class="menu-item {{showGameMenu ? 'active' : ''}}" catchtap="toggleGameMenu">
      <text class="menu-text">选 择(S)</text>
      
      <view class="dropdown-menu bevel-outset" wx:if="{{showGameMenu}}">
        <view class="menu-option" catchtap="menuRestart">
          <text class="option-text">重 来 (F2)</text>
        </view>
        
        <view class="menu-separator"></view>
        
        <view class="menu-option" catchtap="changeDifficulty" data-level="beginner">
          <text class="check-mark">{{currentDifficulty === 'beginner' ? '√' : ''}}</text>
          <text class="option-text">★微笑着流泪★</text>
        </view>
        
        <view class="menu-option" catchtap="changeDifficulty" data-level="intermediate">
          <text class="check-mark">{{currentDifficulty === 'intermediate' ? '√' : ''}}</text>
          <text class="option-text">◆寂寞在唱歌◆</text>
        </view>
        
        <view class="menu-option" catchtap="changeDifficulty" data-level="expert">
          <text class="check-mark">{{currentDifficulty === 'expert' ? '√' : ''}}</text>
          <text class="option-text">☣被伤过的心☣</text>
        </view>

        <view class="menu-separator"></view>

        <view class="menu-option" catchtap="openLeaderboard">
          <text class="option-text">🏆 排行榜</text>
        </view>

        <view class="menu-separator"></view>

        <view class="menu-option" catchtap="exitGame">
          <text class="option-text">离 开(X)</text>
        </view>
      </view>
    </view>

    <view class="menu-item" bindtap="closeMenu">
      <text class="menu-text">帮 助(H)</text>
    </view>
  </view>

  <view class="window-body game-body">
    <view class="status-panel bevel-inset">
      <view class="led-wrapper">
         <text class="led-label">心碎</text>
         <view class="led-counter">{{beaconsLeft < 10 ? '00' + beaconsLeft : (beaconsLeft < 100 ? '0' + beaconsLeft : beaconsLeft)}}</view>
      </view>
      
      <view class="face-btn bevel-outset" bindtap="resetGame">
        {{gameState === 'lost' ? '(T^T)' : (gameState === 'won' ? '(>‿◠)' : '(¬_¬)')}}
      </view>
      
      <view class="led-wrapper">
         <text class="led-label">时间</text>
         <view class="led-counter">{{timeElapsed < 10 ? '00' + timeElapsed : (timeElapsed < 100 ? '0' + timeElapsed : (timeElapsed > 999 ? 999 : timeElapsed))}}</view>
      </view>
    </view>

    <scroll-view scroll-x scroll-y enable-flex class="board-scroll-container bevel-inset">
      <view class="game-board" 
            style="grid-template-columns: repeat({{difficulty[currentDifficulty].cols}}, {{cellSize}}px); width: {{difficulty[currentDifficulty].cols * cellSize}}px;">
        
        <view class="cell {{item.revealed ? 'revealed' : 'unrevealed'}}"
              wx:for="{{grid}}"
              wx:key="index"
              data-row="{{item.row}}"
              data-col="{{item.col}}"
              bindtap="handleTap"
              bindlongpress="handleLongPress"
              style="width: {{cellSize}}px; height: {{cellSize}}px; line-height: {{cellSize}}px;">
          
          <block wx:if="{{!item.revealed}}">
            <text wx:if="{{item.marked}}" class="icon icon-flag">🚬</text>
          </block>
          <block wx:else>
            <text wx:if="{{item.isBlackHole}}" class="icon icon-mine">💔</text>
            <text wx:elif="{{item.count > 0}}" class="number number-{{item.count}}">{{item.count}}</text>
          </block>

        </view>
      </view>
    </scroll-view>

    <view class="emo-footer">
      <text class="emo-text">{{randomQuote}}</text>
    </view>

  </view>
</view>

<!-- 排行榜对话框 -->
<view class="leaderboard-overlay" wx:if="{{showLeaderboard}}" catchtap="closeLeaderboard">
  <view class="leaderboard-dialog" catchtap="">
    <view class="leaderboard-header">
      <text class="leaderboard-title">🏆 排行榜 🏆</text>
      <view class="leaderboard-close" bindtap="closeLeaderboard">×</view>
    </view>

    <!-- 难度切换 Tab -->
    <view class="leaderboard-tabs">
      <view class="lb-tab {{leaderboardTab === 'beginner' ? 'active' : ''}}"
            bindtap="switchLeaderboardTab" data-tab="beginner">
        ★微笑着流泪★
      </view>
      <view class="lb-tab {{leaderboardTab === 'intermediate' ? 'active' : ''}}"
            bindtap="switchLeaderboardTab" data-tab="intermediate">
        ◆寂寞在唱歌◆
      </view>
      <view class="lb-tab {{leaderboardTab === 'expert' ? 'active' : ''}}"
            bindtap="switchLeaderboardTab" data-tab="expert">
        ☣被伤过的心☣
      </view>
    </view>

    <!-- 排行榜列表 -->
    <view class="leaderboard-list">
      <view class="lb-item {{item.isUser ? 'user-item' : ''}}"
            wx:for="{{leaderboardData}}" wx:key="rank">
        <view class="lb-rank">{{item.rank}}</view>
        <view class="lb-avatar">{{item.avatar}}</view>
        <view class="lb-name">{{item.name}}</view>
        <view class="lb-time">{{item.time}}s</view>
      </view>

      <view class="lb-empty" wx:if="{{leaderboardData.length === 0}}">
        <text>暂无记录，快去挑战吧！</text>
      </view>
    </view>

    <!-- 底部提示 -->
    <view class="leaderboard-footer">
      <text class="footer-tip">提示：完成游戏后自动保存最佳成绩</text>
    </view>
  </view>
</view>
</view>