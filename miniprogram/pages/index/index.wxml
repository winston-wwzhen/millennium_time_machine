<view class="desktop">
  <!-- 桌面图标区域 -->
  <view class="icon-grid">
    <block wx:for="{{desktopIcons}}" wx:key="id">
      <view class="desktop-icon" bindtap="onIconTap" data-path="{{item.path}}">
        <view class="icon-img">{{item.icon}}</view>
        <text class="icon-label">{{item.name}}</text>
      </view>
    </block>
  </view>

  <!-- "My Computer" 风格的装饰 - 便签 -->
  <!-- 注意：这里去掉了内联样式，改用 CSS 控制位置 -->
  <view class="widget-container">
    <view class="sticky-note">
      <text>TODO LIST:\n[x] Fix Y2K bugs\n[ ] Feed the Lion\n</text>
    </view>
  </view>

  <!-- 桌面助手：小狮子 (Office Assistant 风格) -->
  <view class="desktop-agent">
    <view class="agent-bubble">
      <text>Welcome back!</text>
    </view>
    <view class="agent-body">🦁</view>
  </view>

  <!-- 任务栏 -->
  <view class="taskbar">
    <view class="start-button {{showStartMenu ? 'active' : ''}}" bindtap="toggleStartMenu">
      <image class="win-logo" src="../../images/borders/border_win98.png" mode="aspectFit"></image>
      <text>Start</text>
    </view>
    
    <view class="taskbar-divider"></view>
    
    <view class="task-items">
      <!-- 运行中的任务 -->
    </view>
    
    <view class="system-tray">
      <view class="tray-icon">🔊</view>
      <view class="system-time">{{systemTime}}</view>
    </view>
  </view>

  <!-- 开始菜单 (弹出层) -->
  <view class="start-menu" wx:if="{{showStartMenu}}">
    <view class="sidebar">
      <text class="os-version">Millennium\nOS 2000</text>
    </view>
    <view class="menu-items">
      <view class="menu-item" bindtap="onIconTap" data-path="/pages/about/index">
        <text class="menu-icon">📂</text> Programs
      </view>
      <view class="menu-item">
        <text class="menu-icon">⚙️</text> Settings
      </view>
      <view class="menu-item">
        <text class="menu-icon">❓</text> Help
      </view>
      <view class="menu-line"></view>
      <view class="menu-item">
        <text class="menu-icon">🔌</text> Shut Down...
      </view>
    </view>
  </view>

  <!-- 彩蛋对话框 -->
  <view class="easter-egg-overlay" wx:if="{{showEasterEgg}}">
    <view class="easter-egg-dialog">
      <view class="dialog-header">
        <text class="dialog-icon">🎉</text>
        <text class="dialog-title">发现彩蛋!</text>
      </view>
      <view class="dialog-body">
        <text class="egg-message">{{easterEggMessage}}</text>
      </view>
      <view class="dialog-footer">
        <button class="win98-btn-std" bindtap="closeEasterEgg">确定</button>
      </view>
    </view>
  </view>
</view>