/* miniprogram/pages/index/index.wxss */

/* 经典 Windows 98 桌面背景色 */
page {
  height: 100vh;
  background-color: #008080;
  font-family: 'Courier New', Courier, monospace;
  overflow: hidden;
  box-sizing: border-box;
  /* 为微信导航栏预留空间 */
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
}

.desktop {
  width: 100%;
  height: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
}

/* 图标网格 - 纵向填充，自动换列（Windows 桌面风格） */
.icon-grid {
  flex: 1;
  display: flex;
  flex-direction: column;
  flex-wrap: wrap;
  align-content: flex-start;
  padding: 20rpx;
  padding-top: 180rpx;
  gap: 20rpx;
  overflow-y: auto;
}

.desktop-icon {
  width: 110rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 10rpx;
}

.desktop-icon:active .icon-img {
  filter: brightness(0.8);
  transform: translate(1px, 1px); /* 点击下沉效果 */
}
.desktop-icon:active .icon-label {
  background-color: #000080;
  color: #fff;
  border: 1px dotted #fff;
}

.icon-img {
  font-size: 70rpx;
  width: 90rpx;
  height: 90rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 10rpx;
  filter: drop-shadow(2px 2px 0 rgba(0,0,0,0.3));
}

.icon-label {
  color: white;
  font-size: 22rpx;
  text-align: center;
  text-shadow: 2px 2px 0 #000, 0 0 4px rgba(0,0,0,0.8);
  line-height: 1.3;
  padding: 4rpx 8rpx;
  font-weight: 500;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 120rpx;
}

/* 桌面助手 (小狮子) - 新增样式 */
.desktop-agent {
  position: absolute;
  right: 30rpx;
  bottom: 100rpx;
  z-index: 50;
  display: flex;
  flex-direction: column;
  align-items: center;
  /* transform 由 JS 动态控制 */
  will-change: transform;
  touch-action: none; /* 阻止默认触摸行为 */
}

.desktop-agent.dragging {
  animation: none;
}

.agent-body {
  width: 160rpx;
  height: 160rpx;
  font-size: 160rpx;
  filter: drop-shadow(2px 2px 0 rgba(0,0,0,0.3));
  transition: transform 0.3s ease;
}

/* 小狮子的不同心情状态 */
.agent-body.agent-happy {
  animation: bounce 0.5s ease;
}

.agent-body.agent-sleepy {
  animation: sleepy 2s ease-in-out infinite;
  filter: drop-shadow(2px 2px 0 rgba(0,0,0,0.3)) grayscale(0.3);
}

.agent-body.agent-surprised {
  animation: surprise 0.3s ease;
}

.agent-body.agent-normal {
  animation: float 3s ease-in-out infinite;
}

/* 小狮子跳舞状态 - 彩蛋 */
.agent-body.agent-dancing {
  animation: dance 0.8s ease-in-out infinite;
}

/* 小狮子午夜发光状态 - 彩蛋 */
.desktop-agent.midnight-glow .agent-body {
  filter: drop-shadow(0 0 20rpx rgba(255, 215, 0, 0.8)) drop-shadow(0 0 40rpx rgba(255, 215, 0, 0.6));
  animation: glow-pulse 2s ease-in-out infinite;
}

@keyframes glow-pulse {
  0%, 100% {
    filter: drop-shadow(0 0 20rpx rgba(255, 215, 0, 0.8)) drop-shadow(0 0 40rpx rgba(255, 215, 0, 0.6));
  }
  50% {
    filter: drop-shadow(0 0 30rpx rgba(255, 215, 0, 1)) drop-shadow(0 0 60rpx rgba(255, 215, 0, 0.8));
  }
}

@keyframes bounce {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.2) rotate(-10deg); }
}

@keyframes sleepy {
  0%, 100% { transform: rotate(-5deg); }
  50% { transform: rotate(5deg); }
}

@keyframes surprise {
  0% { transform: scale(1); }
  50% { transform: scale(1.3); }
  100% { transform: scale(1); }
}

/* 跳舞动画 - 彩蛋专用 */
@keyframes dance {
  0% { transform: translateX(0) rotate(0deg); }
  25% { transform: translateX(-10rpx) rotate(-15deg) scale(1.1); }
  50% { transform: translateX(10rpx) rotate(15deg) scale(1.1); }
  75% { transform: translateX(-10rpx) rotate(-15deg) scale(1.1); }
  100% { transform: translateX(0) rotate(0deg); }
}

.agent-bubble {
  background: #ffffe0;
  border: 1px solid #000;
  border-radius: 8rpx;
  padding: 8rpx 16rpx;
  font-size: 20rpx;
  margin-bottom: 6rpx;
  position: relative;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.2);
  white-space: nowrap;
  opacity: 0;
  transform: scale(0.8);
  transition: all 0.3s ease;
}

.agent-bubble.show {
  opacity: 1;
  transform: scale(1);
}

/* 气泡的小箭头 */
.agent-bubble::after {
  content: '';
  position: absolute;
  bottom: -8rpx;
  left: 50%;
  transform: translateX(-50%);
  border-width: 8rpx 8rpx 0;
  border-style: solid;
  border-color: #000 transparent transparent transparent;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

/* 任务栏 */
.taskbar {
  /* 适配底部安全区 */
  height: calc(80rpx + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom);

  background-color: #c0c0c0;
  border-top: 2px solid #fff;
  display: flex;
  align-items: center;
  padding-left: 6rpx;
  padding-right: 6rpx;
  box-sizing: border-box;
  z-index: 100;
  position: relative;
  box-shadow: 0 -2px 5px rgba(0,0,0,0.2);
}

.start-button {
  display: flex;
  align-items: center;
  padding: 4rpx 12rpx;
  background-color: #c0c0c0;
  border: 2px outset #fff;
  font-weight: bold;
  font-size: 28rpx;
  margin-right: 12rpx;
  height: 52rpx;
  box-shadow: 1px 1px 0 #000;
  position: relative;
  z-index: 1;
}

.start-button:active, .start-button.active {
  border: 2px inset #fff;
  background-color: #d0d0d0;
  box-shadow: none;
  transform: translate(1px, 1px);
}

.win-logo {
  font-size: 36rpx;
  margin-right: 10rpx;
}

.taskbar-divider {
  width: 2px;
  height: 50rpx;
  border-left: 1px solid #808080;
  border-right: 1px solid #fff;
  margin: 0 10rpx;
}

.task-items {
  flex: 1;
}

.system-tray {
  border: 2px inset #fff;
  padding: 0 14rpx;
  height: 50rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 22rpx;
  background: #c0c0c0;
  box-shadow: inset 1px 1px 0 #000;
}

.tray-icon {
  font-size: 28rpx;
  cursor: pointer;
}

/* 网络状态图标 */
.network-icon {
  transition: all 0.3s ease;
  position: relative;
}

.network-icon.online {
  filter: drop-shadow(0 0 2rpx #00ff00);
}

.network-icon.offline {
  filter: drop-shadow(0 0 2rpx #ff0000);
  animation: blink 1s ease-in-out infinite;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

/* 网络图标容器 */
.network-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

.volume-wrapper {
  position: relative;
  display: flex;
  align-items: center;
}

/* QCIO图标 */
.qcio-icon {
  transition: all 0.2s ease;
}

.qcio-icon:active {
  transform: scale(0.9);
  filter: brightness(0.8);
}

/* 网络信息气泡 */
.network-bubble {
  position: absolute;
  bottom: 60rpx;
  right: 0;
  background: #ffffe0;
  border: 2px solid;
  border-color: #808080 #ffffff #ffffff #808080;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.3);
  padding: 8rpx 12rpx;
  min-width: 220rpx;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  opacity: 0;
  transform: scale(0.8);
  transform-origin: bottom right;
  transition: all 0.2s ease;
  pointer-events: none;
}

.network-bubble.show {
  opacity: 1;
  transform: scale(1);
}

.network-bubble::after {
  content: '';
  position: absolute;
  bottom: -10rpx;
  right: 12rpx;
  border-width: 10rpx 10rpx 0;
  border-style: solid;
  border-color: #808080 transparent transparent transparent;
}

.network-status-text {
  font-size: 22rpx;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  border-bottom: 1px solid #808080;
  padding-bottom: 4rpx;
  margin-bottom: 4rpx;
}

.network-speed-text {
  font-size: 20rpx;
  color: #333;
  font-family: 'SimSun', serif;
}

/* 音量气泡 */
.volume-bubble {
  position: absolute;
  bottom: 60rpx;
  right: 0;
  background: #ffffe0;
  border: 2px solid;
  border-color: #808080 #ffffff #ffffff #808080;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.3);
  padding: 8rpx 12rpx;
  min-width: 140rpx;
  opacity: 0;
  transform: scale(0.8);
  transform-origin: bottom right;
  transition: all 0.2s ease;
  pointer-events: none;
}

.volume-bubble.show {
  opacity: 1;
  transform: scale(1);
}

.volume-bubble::after {
  content: '';
  position: absolute;
  bottom: -10rpx;
  right: 12rpx;
  border-width: 10rpx 10rpx 0;
  border-style: solid;
  border-color: #808080 transparent transparent transparent;
}

.volume-status-text {
  font-size: 22rpx;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  text-align: center;
}

/* ==================== 网管系统插件 ==================== */
.network-plugin-panel {
  position: absolute;
  top: 180rpx;
  right: 20rpx;
  width: 220rpx;
  background: #f5f0dc;
  border-radius: 2rpx;
  box-shadow:
    2px 2px 5px rgba(0,0,0,0.15),
    inset 0 0 30px rgba(0,0,0,0.02);
  z-index: 80; /* 比桌面应用(2000+)低，始终在底层 */
  opacity: 0.85;
}

/* 纸质纹理效果 */
.network-plugin-panel::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image:
    repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.01) 2px,
      rgba(0,0,0,0.01) 4px
    );
  pointer-events: none;
}

.plugin-title-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 6rpx 10rpx;
  border-bottom: 1px solid rgba(0,0,0,0.08);
}

.plugin-title-text {
  color: #6b5d4f;
  font-size: 20rpx;
  font-weight: normal;
  font-family: 'SimSun', serif;
  letter-spacing: 1rpx;
  opacity: 0.8;
}

.plugin-close-btn {
  color: #8b7d6b;
  font-size: 28rpx;
  line-height: 1;
  padding: 0 4rpx;
  cursor: pointer;
  opacity: 0.6;
  transition: opacity 0.15s ease;
}

.plugin-close-btn:active {
  opacity: 1;
}

.plugin-body {
  padding: 10rpx;
}

.plugin-row {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx;
  margin-bottom: 6rpx;
  background: rgba(255,255,255,0.5);
  border-radius: 2rpx;
  border: 1px solid rgba(0,0,0,0.05);
  transition: all 0.15s ease;
}

.plugin-row:last-child {
  margin-bottom: 0;
}

.plugin-row:active {
  background: rgba(107,93,79,0.15);
  border-color: rgba(107,93,79,0.2);
}

.plugin-row-icon {
  font-size: 24rpx;
  flex-shrink: 0;
  opacity: 0.7;
}

.plugin-row-info {
  display: flex;
  flex-direction: column;
  gap: 2rpx;
  flex: 1;
  min-width: 0;
}

.plugin-row-label {
  font-size: 18rpx;
  color: #8b7d6b;
  font-family: 'SimSun', serif;
  line-height: 1;
  letter-spacing: 0.5rpx;
  font-weight: 400;
}

.plugin-row-value {
  font-size: 22rpx;
  font-weight: 500;
  color: #5a4d42;
  font-family: 'Arial', sans-serif;
  line-height: 1.2;
  letter-spacing: 0.3rpx;
}

/* 开始菜单遮罩 - 点击外部关闭菜单 */
.start-menu-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9998;
  background: transparent;
}

/* 开始菜单 - 经典Win98单栏布局 */
.start-menu {
  position: absolute;
  bottom: calc(82rpx + env(safe-area-inset-bottom));
  left: 6rpx;
  width: 320rpx;
  max-height: 70vh;
  background-color: #c0c0c0;
  border-top: 2px solid #fff;
  border-left: 2px solid #fff;
  border-right: 2px solid #808080;
  border-bottom: 2px solid #808080;
  box-shadow: 4px 4px 0 rgba(0,0,0,0.4);
  display: flex;
  flex-direction: column;
  z-index: 9999;
  animation: slideUp 0.12s ease-out;
  overflow: hidden;
}

@keyframes slideUp {
  from {
    transform: translateY(8rpx);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

/* 用户横幅 */
.user-banner {
  background: linear-gradient(180deg, #000080, #1084d0);
  padding: 12rpx 16rpx;
  display: flex;
  align-items: center;
  gap: 10rpx;
  border-bottom: 1px solid #808080;
}

.user-avatar {
  width: 48rpx;
  height: 48rpx;
  background: #fff;
  border-top: 1px solid #808080;
  border-left: 1px solid #808080;
  border-right: 1px solid #fff;
  border-bottom: 1px solid #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  box-shadow: inset 1px 1px 0 #0a0a0a, inset -1px -1px 0 #dfdfdf;
}

.user-info {
  display: flex;
  flex-direction: column;
  gap: 2rpx;
}

.user-name {
  color: #fff;
  font-size: 24rpx;
  font-weight: bold;
  text-shadow: 1px 1px 0 rgba(0,0,0,0.3);
}

.user-status {
  color: #a0d0ff;
  font-size: 18rpx;
}

/* 菜单项容器 */
.menu-items {
  padding: 6rpx;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

/* 菜单项 */
.menu-item {
  padding: 10rpx 14rpx;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  color: #000;
  margin-bottom: 2rpx;
}

.menu-item:active {
  background-color: #000080;
  color: white;
}

.menu-item.selected {
  background-color: #000080;
  color: white;
}

.menu-item:active .menu-icon,
.menu-item:active .menu-text,
.menu-item:active .submenu-arrow {
  color: white;
}

.menu-item.selected .menu-icon,
.menu-item.selected .menu-text,
.menu-item.selected .submenu-arrow {
  color: white;
}

.menu-icon {
  width: 38rpx;
  text-align: center;
  margin-right: 10rpx;
  font-size: 28rpx;
  flex-shrink: 0;
}

.menu-text {
  flex: 1;
  font-family: 'SimSun', serif;
}

/* 子菜单样式 */
.submenu {
  position: relative;
}

.submenu-arrow {
  font-size: 18rpx;
  color: #666;
  transition: transform 0.15s ease;
  margin-left: 6rpx;
}

.submenu-arrow.expanded {
  transform: rotate(90deg);
}

/* 程序子菜单 - 独立弹出层 */
.submenu-popup {
  position: absolute;
  bottom: calc(240rpx + env(safe-area-inset-bottom));
  left: 330rpx;
  width: 200rpx;
  background-color: #c0c0c0;
  border-top: 2px solid #fff;
  border-left: 2px solid #fff;
  border-right: 2px solid #808080;
  border-bottom: 2px solid #808080;
  box-shadow: 4px 4px 0 rgba(0,0,0,0.4);
  display: none;
  z-index: 10000;
  animation: slideInRight 0.12s ease-out;
  padding: 6rpx;
}

.submenu-popup.show {
  display: block;
}

@keyframes slideInRight {
  from {
    transform: translateX(-8rpx);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.submenu-item {
  padding: 10rpx 14rpx;
  font-size: 22rpx;
  display: flex;
  align-items: center;
  color: #000;
  margin-bottom: 2rpx;
  font-family: 'SimSun', serif;
}

.submenu-item:active {
  background-color: #000080;
  color: white;
}

.submenu-icon {
  width: 34rpx;
  text-align: center;
  margin-right: 8rpx;
  font-size: 24rpx;
  flex-shrink: 0;
}

.submenu-text {
  flex: 1;
}

/* 关机项特殊样式 */
.shutdown-item {
  margin-top: 4rpx;
}

/* 分隔线 */
.menu-line {
  border-top: 1px solid #808080;
  border-bottom: 1px solid #fff;
  margin: 6rpx 10rpx;
  height: 0;
}

/* 右键菜单 */
.context-menu-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 9997;
  background: transparent;
}

.context-menu {
  position: fixed;
  background: #c0c0c0;
  border: 2px outset #fff;
  box-shadow: 4px 4px 0 rgba(0,0,0,0.5);
  z-index: 9998;
  min-width: 200rpx;
  padding: 2rpx;
  animation: contextFadeIn 0.15s ease-out;
}

@keyframes contextFadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.context-menu-item {
  padding: 12rpx 20rpx;
  font-size: 26rpx;
  display: flex;
  align-items: center;
  color: #000;
  margin-bottom: 2rpx;
  font-weight: 500;
}

.context-menu-item:active {
  background-color: #000080;
  color: white;
}

.context-icon {
  width: 40rpx;
  text-align: center;
  margin-right: 12rpx;
  font-size: 32rpx;
}

/* Win98 错误弹窗 */
.win98-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 20000;
}

.win98-window {
  background: #c0c0c0;
  border-top: 1px solid #ffffff;
  border-left: 1px solid #ffffff;
  border-right: 1px solid #808080;
  border-bottom: 1px solid #808080;
  box-shadow: inset 1px 1px #dfdfdf, inset -1px -1px #0a0a0a;
  display: flex;
  flex-direction: column;
}

.win98-title-bar {
  background: linear-gradient(90deg, #000080, #1084d0);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 2px 3px 2px 6px;
  height: 20px;
}

.win98-title-text {
  color: white;
  font-weight: bold;
  font-size: 12px;
  font-family: 'SimSun', 'Courier New', monospace;
}

.win98-title-controls {
  display: flex;
  gap: 2px;
}

.win98-title-controls button {
  all: unset;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 16px;
  height: 14px;
  background: #c0c0c0;
  border-top: 1px solid #ffffff;
  border-left: 1px solid #ffffff;
  border-right: 1px solid #808080;
  border-bottom: 1px solid #808080;
  font-size: 10px;
  color: #000;
  padding: 0;
  margin: 0;
}

.win98-btn-close::after {
  content: "×";
  font-size: 14px;
  font-weight: bold;
}

.win98-window-body {
  padding: 0;
  display: flex;
  flex-direction: column;
}

.error-dialog {
  width: 320px;
}

.error-dialog-content {
  padding: 20px 16px;
  display: flex;
  gap: 12px;
  align-items: flex-start;
}

.error-icon {
  font-size: 40px;
  flex-shrink: 0;
}

.error-messages {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.error-title {
  font-size: 14px;
  font-weight: bold;
  color: #000;
  font-family: 'SimSun', serif;
}

.error-text {
  font-size: 12px;
  color: #333;
  font-family: 'SimSun', serif;
  line-height: 1.5;
}

.error-code {
  font-size: 11px;
  color: #cc0000;
  font-family: 'Courier New', monospace;
  margin-top: 4px;
}

.dialog-actions {
  display: flex;
  justify-content: center;
  padding: 8px 16px 12px;
  background: #c0c0c0;
}

.win98-btn-std {
  font-size: 12px;
  color: #000;
  height: 24px;
  padding: 0 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #c0c0c0;
  border-top: 1px solid #ffffff;
  border-left: 1px solid #ffffff;
  border-right: 1px solid #808080;
  border-bottom: 1px solid #808080;
  font-family: 'SimSun', serif;
}

.win98-btn-std:active {
  border-top: 1px solid #808080;
  border-left: 1px solid #808080;
  border-right: 1px solid #ffffff;
  border-bottom: 1px solid #ffffff;
}

/* ==================== 系统时间 ==================== */
.system-time {
  font-family: 'SimSun', serif;
  cursor: pointer;
  padding: 4rpx 8rpx;
}

.system-time:active {
  background: rgba(0, 0, 128, 0.3);
}

/* 隐蔽的作者签名 - 任务栏中央 */
.author-signature {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  font-size: 16rpx;
  color: rgba(128, 128, 128, 0.4);
  font-family: 'Arial', sans-serif;
  letter-spacing: 1rpx;
  pointer-events: none;
  user-select: none;
}

/* ==================== 日历弹窗 ==================== */
.date-dialog {
  position: absolute;
  bottom: calc(84rpx + env(safe-area-inset-bottom));
  right: 6rpx;
  width: 260px;
  background: #c0c0c0;
  border-top: 2px solid #fff;
  border-left: 2px solid #fff;
  border-right: 2px solid #808080;
  border-bottom: 2px solid #808080;
  box-shadow: 2px 2px 0 rgba(0,0,0,0.3);
  z-index: 9999;
  animation: calendarPop 0.15s ease-out;
}

@keyframes calendarPop {
  from {
    opacity: 0;
    transform: translateY(8rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.date-dialog-content {
  padding: 8px;
}

.calendar-display {
  background: #fff;
  border: 2px inset #fff;
  padding: 8px;
  margin-bottom: 8px;
  text-align: center;
}

.calendar-header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
  margin-bottom: 4px;
}

.calendar-year,
.calendar-month {
  font-size: 18px;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
}

.calendar-title {
  font-size: 14px;
  color: #333;
  font-family: 'SimSun', serif;
}

.calendar-day {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 4px 0;
}

.day-number {
  font-size: 48px;
  font-weight: bold;
  color: #000;
  font-family: 'Arial', sans-serif;
  line-height: 1;
}

.day-name {
  font-size: 12px;
  color: #666;
  font-family: 'SimSun', serif;
}

/* 星期标题行 */
.weekday-header {
  display: flex;
  justify-content: space-around;
  padding: 4px 0;
  border-bottom: 1px solid #808080;
  margin-bottom: 4px;
}

.weekday-title {
  font-size: 11px;
  color: #000080;
  font-family: 'SimSun', serif;
  font-weight: bold;
}

/* 日历网格 */
.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 2px;
  padding: 4px 0;
}

.calendar-day-cell {
  aspect-ratio: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-family: 'SimSun', serif;
  border-radius: 2px;
}

.calendar-day-cell.today {
  background: #000080;
  color: #fff;
  font-weight: bold;
  border-radius: 4px;
}

.date-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding-top: 4px;
  border-top: 1px solid #808080;
}

.info-row {
  display: flex;
  align-items: center;
  font-size: 12px;
  font-family: 'SimSun', serif;
}

.info-label {
  width: 50px;
  color: #666;
}

.info-value {
  color: #000;
}

/* ==================== 蓝屏彩蛋样式 ==================== */

/* 桌面蓝屏状态 */
.desktop.blue-screen {
  background-color: #0000FF !important;
}

/* 桌面刷新状态 - Win98风格闪烁效果 */
.desktop.refreshing {
  animation: desktopFlash 0.4s ease-in-out;
}

@keyframes desktopFlash {
  0% {
    opacity: 1;
    filter: brightness(1);
  }
  25% {
    opacity: 0.3;
    filter: brightness(1.5);
  }
  50% {
    opacity: 0.1;
    filter: brightness(2);
    background-color: #ffffff !important;
  }
  75% {
    opacity: 0.3;
    filter: brightness(1.5);
  }
  100% {
    opacity: 1;
    filter: brightness(1);
  }
}

/* 刷新时图标网格淡出淡入效果 */
.desktop.refreshing .icon-grid {
  animation: gridFade 0.8s ease-in-out;
}

@keyframes gridFade {
  0% {
    opacity: 1;
  }
  30% {
    opacity: 0.2;
  }
  70% {
    opacity: 0.2;
  }
  100% {
    opacity: 1;
  }
}

/* 蓝屏覆盖层 */
.blue-screen-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #0000AA;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

.bsod-content {
  display: flex;
  flex-direction: column;
  gap: 30rpx;
  padding: 60rpx;
}

.bsod-title {
  font-size: 48rpx;
  font-weight: bold;
  color: #FFFFFF;
  font-family: 'Courier New', monospace;
  background-color: #0000AA;
}

.bsod-text {
  font-size: 28rpx;
  color: #FFFFFF;
  font-family: 'Courier New', monospace;
}

.bsod-code {
  font-size: 24rpx;
  color: #FFFFFF;
  font-family: 'Courier New', monospace;
}

.bsod-hint {
  font-size: 24rpx;
  color: #FFFF00;
  font-family: 'SimSun', serif;
  margin-top: 20rpx;
}

/* ==================== 隐藏彩蛋样式 ==================== */

/* 桌面右下角隐藏触发区域 */
.hidden-trigger {
  position: absolute;
  bottom: 10rpx;
  right: 10rpx;
  width: 60rpx;
  height: 60rpx;
  z-index: 1;
  opacity: 0;
}

/* 隐藏图标 */
.hidden-icon .icon-img {
  filter: drop-shadow(0 0 10rpx rgba(147, 51, 234, 0.8));
  animation: hidden-glow 2s ease-in-out infinite;
}

@keyframes hidden-glow {
  0%, 100% {
    filter: drop-shadow(0 0 10rpx rgba(147, 51, 234, 0.8));
  }
  50% {
    filter: drop-shadow(0 0 20rpx rgba(147, 51, 234, 1)) drop-shadow(0 0 30rpx rgba(233, 30, 99, 0.8));
  }
}

/* ==================== 用户编辑弹窗样式 ==================== */

/* 用户横幅可点击样式 */
.user-banner {
  cursor: pointer;
}

.user-banner:active {
  opacity: 0.8;
}

/* 用户编辑弹窗 */
.user-edit-dialog {
  position: absolute;
  bottom: calc(82rpx + env(safe-area-inset-bottom));
  left: 6rpx;
  width: 520rpx;
  background: #c0c0c0;
  border-top: 2px solid #fff;
  border-left: 2px solid #fff;
  border-right: 2px solid #808080;
  border-bottom: 2px solid #808080;
  box-shadow: 4px 4px 0 rgba(0,0,0,0.4);
  z-index: 10001;
  animation: slideUp 0.12s ease-out;
}

.user-edit-content {
  padding: 16rpx;
}

.edit-section {
  margin-bottom: 20rpx;
}

.edit-label {
  display: block;
  font-size: 24rpx;
  color: #000;
  margin-bottom: 10rpx;
  font-family: 'SimSun', serif;
  font-weight: bold;
}

/* 头像网格 */
.avatar-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 10rpx;
  padding: 10rpx;
  background: #fff;
  border: 2px inset #fff;
}

.avatar-choice {
  aspect-ratio: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  background: #c0c0c0;
  border-top: 1px solid #fff;
  border-left: 1px solid #fff;
  border-right: 1px solid #808080;
  border-bottom: 1px solid #808080;
  cursor: pointer;
  transition: all 0.1s ease;
}

.avatar-choice:active {
  border-top: 1px solid #808080;
  border-left: 1px solid #808080;
  border-right: 1px solid #fff;
  border-bottom: 1px solid #fff;
  transform: translate(1px, 1px);
}

.avatar-choice.selected {
  background: #000080;
  border-top: 2px solid #fff;
  border-left: 2px solid #fff;
  border-right: 2px solid #808080;
  border-bottom: 2px solid #808080;
  box-shadow: inset 0 0 0 2px #000080;
}

/* 昵称输入 */
.nickname-input {
  width: 100%;
  height: 70rpx;
  box-sizing: border-box;
  padding: 0 12rpx;
  font-size: 26rpx;
  font-family: 'SimSun', serif;
  background: #fff;
  border: 2px inset #fff;
  color: #000;
  line-height: 70rpx;
}

.nickname-input:focus {
  outline: none;
  background: #fff;
}

/* ==================== 系统信息弹窗 ==================== */
.system-info-dialog {
  width: 320px;
}

.system-info-content {
  padding: 20px 16px;
}

.info-title {
  font-size: 14px;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  display: block;
  margin-bottom: 12px;
  text-align: center;
}

.info-list {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.info-item {
  display: flex;
  justify-content: space-between;
  font-size: 12px;
  font-family: 'SimSun', serif;
  align-items: center;
}

.info-label {
  color: #666;
  font-weight: 500;
  white-space: nowrap;
  flex-shrink: 0;
}

.info-value {
  color: #000;
  font-weight: 500;
  text-align: right;
  white-space: nowrap;
}

/* ==================== 助手设置弹窗 ==================== */
.agent-settings-dialog {
  width: 300px;
}

.agent-settings-content {
  padding: 20px 16px;
  display: flex;
  justify-content: center;
}

.settings-text {
  font-size: 13px;
  color: #333;
  font-family: 'SimSun', serif;
  line-height: 1.8;
  white-space: pre-line;
}

/* ==================== 关于弹窗 ==================== */
.about-dialog {
  width: 360px;
}

.about-content {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-height: 350px;
}

.about-title {
  font-size: 15px;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  display: block;
  margin-bottom: 8px;
  text-align: center;
}

.about-text {
  font-size: 12px;
  color: #333;
  font-family: 'SimSun', serif;
  text-align: center;
  margin-bottom: 8px;
}

.about-section {
  margin-top: 12px;
  padding-top: 8px;
  border-top: 1px solid #c0c0c0;
}

.about-section-title {
  font-size: 13px;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  display: block;
  margin-bottom: 6px;
}

.about-features {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.about-feature {
  font-size: 11px;
  color: #000;
  font-family: 'SimSun', serif;
  text-align: left;
  padding-left: 4px;
}

/* ========================================
   Windows 11 风格关机弹窗
   ======================================== */

.win11-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(10px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  opacity: 0;
  visibility: hidden;
  transition: all 0.2s ease;
}

.win11-overlay.show {
  opacity: 1;
  visibility: visible;
}

.win11-dialog {
  width: 330px;
  max-width: 90%;
  background: #ffffff;
  border-radius: 12px;
  border: 1px solid rgba(0, 0, 0, 0.08);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15), 0 2px 8px rgba(0, 0, 0, 0.1);
  padding: 36px 32px 28px;
  display: flex;
  flex-direction: column;
  align-items: center;
  animation: win11SlideIn 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes win11SlideIn {
  from {
    opacity: 0;
    transform: scale(0.95) translateY(-10px);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

.win11-icon {
  font-size: 48px;
  margin-bottom: 16px;
}

.win11-title {
  font-size: 20px;
  font-weight: 600;
  color: #000000;
  margin-bottom: 20px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.win11-content {
  width: 100%;
  margin-bottom: 24px;
  padding: 0 8px;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.win11-text {
  font-size: 14px;
  line-height: 1.6;
  color: #1a1a1a;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  text-align: left;
  width: 100%;
}

.win11-spacer {
  height: 8px;
}

.win11-emphasis {
  font-weight: 600;
  color: #0078d4;
}

.win11-actions {
  display: flex;
  gap: 12px;
  width: 100%;
}

.win11-btn {
  flex: 1;
  height: 36px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.15s ease;
  cursor: pointer;
}

.win11-btn::after {
  border: none;
}

.win11-btn-secondary {
  background: #f3f3f3;
  color: #1a1a1a;
}

.win11-btn-secondary:active {
  background: #e5e5e5;
}

.win11-btn-primary {
  background: #0078d4;
  color: #ffffff;
}

.win11-btn-primary:active {
  background: #006cbd;
}

/* ==================== 欢迎弹窗 - 时间穿越提示 ==================== */

.welcome-dialog {
  width: 380px;
  max-width: 90vw;
  box-sizing: border-box;
}

.welcome-content {
  padding: 18px 18px;
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-height: 70vh;
  word-wrap: break-word;
  word-break: break-word;
  box-sizing: border-box;
}

.welcome-title {
  font-size: 14px;
  font-weight: bold;
  color: #000080;
  font-family: 'SimSun', serif;
  display: block;
  text-align: center;
  line-height: 1.5;
  word-wrap: break-word;
  word-break: break-word;
  box-sizing: border-box;
  padding: 0 2px;
}

.welcome-subtitle {
  font-size: 13px;
  color: #4a0080;
  font-family: 'SimSun', serif;
  display: block;
  text-align: center;
  line-height: 1.4;
  word-wrap: break-word;
  word-break: break-word;
  box-sizing: border-box;
  padding: 6px 12px;
  margin: 4px 0;
  font-style: italic;
}

.welcome-section {
  margin: 6px 0;
  padding: 10px;
  background: linear-gradient(135deg, rgba(255, 215, 0, 0.15) 0%, rgba(255, 193, 7, 0.1) 100%);
  border: 2px solid #daa520;
  border-radius: 8px;
  text-align: center;
  box-sizing: border-box;
  width: 100%;
}

.welcome-highlight {
  font-size: 15px;
  font-weight: bold;
  color: #b8860b;
  font-family: 'SimSun', serif;
  text-shadow: 1px 1px 0 rgba(255, 255, 255, 0.5);
  word-wrap: break-word;
  word-break: break-word;
  display: block;
  box-sizing: border-box;
  padding: 0 4px;
}

.welcome-text {
  font-size: 12px;
  color: #333;
  font-family: 'SimSun', serif;
  line-height: 1.5;
  text-align: justify;
  text-indent: 2em;
  word-wrap: break-word;
  word-break: break-word;
  display: block;
  box-sizing: border-box;
  padding: 0 2px;
}

.welcome-tips {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin: 6px 0;
  box-sizing: border-box;
}

.welcome-tip {
  font-size: 12px;
  color: #444;
  font-family: 'SimSun', serif;
  line-height: 1.5;
  padding: 8px 10px;
  background: rgba(0, 128, 255, 0.05);
  border-left: 3px solid #000080;
  border-radius: 0 4px 4px 0;
  word-wrap: break-word;
  word-break: break-word;
  box-sizing: border-box;
  display: block;
  width: 100%;
}

.welcome-footer {
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid #c0c0c0;
  display: flex;
  flex-direction: column;
  gap: 6px;
  box-sizing: border-box;
}

.welcome-footer-text {
  font-size: 13px;
  color: #000080;
  font-family: 'SimSun', serif;
  font-weight: bold;
  text-align: center;
  line-height: 1.4;
  word-wrap: break-word;
  word-break: break-word;
  display: block;
  box-sizing: border-box;
  padding: 0 2px;
}

/* 欢迎弹窗主按钮样式 - 2006年代复古风格 */
.welcome-btn-primary {
  background: linear-gradient(180deg, #5a8eea 0%, #3d7dd8 15%, #2e6bcc 50%, #1e5bb0 85%, #154a94 100%);
  color: #fff;
  border: none;
  border-radius: 6px;
  font-weight: bold;
  font-size: 14px;
  height: 42px;
  line-height: 42px;
  padding: 0 28px;
  box-shadow:
    0 1px 0 rgba(255, 255, 255, 0.4) inset,
    0 -1px 0 rgba(0, 0, 0, 0.2) inset,
    0 4px 12px rgba(30, 91, 176, 0.4),
    0 2px 4px rgba(0, 0, 0, 0.2);
  text-shadow: 0 -1px 1px rgba(0, 0, 0, 0.3);
  letter-spacing: 0.5px;
  transition: all 0.15s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.welcome-btn-primary:active {
  background: linear-gradient(180deg, #1e5bb0 0%, #2e6bcc 50%, #3d7dd8 100%);
  box-shadow:
    0 1px 0 rgba(255, 255, 255, 0.2) inset,
    0 -1px 0 rgba(0, 0, 0, 0.3) inset,
    0 2px 6px rgba(30, 91, 176, 0.3),
    0 1px 2px rgba(0, 0, 0, 0.3);
  transform: translateY(1px);
}

/* 欢迎弹窗滚动条隐藏 */
.welcome-content::-webkit-scrollbar {
  display: none;
  width: 0;
  height: 0;
}
