/* 全局布局 */
page {
  height: 100vh;
  background-color: #c0c0c0;
  font-family: 'Courier New', Courier, monospace;
  overflow: hidden;
  
  /* 使用 Flex 布局管理垂直空间，比单纯的 height: 100% 更稳定 */
  display: flex;
  flex-direction: column;
  box-sizing: border-box;

  /* * 顶部避让逻辑：
   * status-bar-height (状态栏) + 44px (标准胶囊按钮区域高度)
   * 这样浏览器窗口正好从胶囊按钮下方开始
   */
  padding-top: calc(env(safe-area-inset-top) + 44px);
  /* 底部避让逻辑：适配全面屏底部的 Home Indicator */
  padding-bottom: env(safe-area-inset-bottom);
}

.win98-window {
  /* Flex: 1 让窗口自动占据页面剩余的所有空间 */
  flex: 1;
  display: flex;
  flex-direction: column;
  /* 用户调整：增加额外的顶部间距，确保窗口完全避开胶囊按钮并产生悬浮感 */
  margin-top: 80rpx;
  padding: 2px;
  box-sizing: border-box;
  
  /* 增加阴影，使其看起来更像浮在桌面上的窗口 */
  box-shadow: 2px 2px 10px rgba(0,0,0,0.3);
  border: 2px solid #fff;
  border-right-color: #000;
  border-bottom-color: #000;
  background-color: #c0c0c0;
  
  /* 确保内部滚动，防止窗口溢出 */
  overflow: hidden;
}

/* 1. 标题栏 */
.title-bar {
  display: flex;
  background: linear-gradient(90deg, #000080, #1084d0);
  padding: 4px;
  color: white;
  align-items: center;
  height: 28px;
  flex-shrink: 0; /* 防止被压缩 */
  justify-content: space-between; /* 确保标题和按钮两端对齐 */
}

.title-icon {
  margin-right: 5px;
  font-size: 14px;
}

.title-text { 
  flex: 1; 
  font-weight: bold; 
  font-size: 14px; 
  white-space: nowrap; 
  overflow: hidden; 
  text-overflow: ellipsis;
  line-height: 20px; /* 垂直居中 */
}

.title-controls {
  display: flex;
  gap: 2px;
  align-items: center;
}

.control-btn {
  width: 18px; 
  height: 18px;
  background-color: #c0c0c0;
  border: 1px outset #fff;
  color: black;
  display: flex; /* 使用 flex 居中文字 */
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: bold;
  line-height: 1;
  padding-bottom: 2px; /* 微调文字位置 */
  box-sizing: border-box;
}

.control-btn:active { 
  border-style: inset; 
  padding-top: 2px; /* 按下时文字下沉效果 */
  padding-bottom: 0;
}

.control-btn.close {
  margin-left: 2px; /* 关闭按钮稍微分开一点 */
}

/* 2. 工具栏 */
.toolbar {
  display: flex;
  padding: 4px;
  gap: 4px;
  border-bottom: 1px solid #808080;
  align-items: center;
  flex-shrink: 0; /* 防止被压缩 */
}

.tool-btn {
  border: 1px outset #fff;
  padding: 2px 8px;
  font-size: 16px;
  background: #c0c0c0;
  display: flex;
  align-items: center;
  justify-content: center;
}
.tool-btn:active { border-style: inset; }
.tool-btn.disabled { color: #808080; border-style: solid; border-color: #c0c0c0; }

/* 刷新按钮旋转动画 */
.tool-btn.refreshing {
  animation: rotate-refresh 0.6s ease-in-out;
}

@keyframes rotate-refresh {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

.tool-separator { width: 1px; height: 20px; background: #808080; border-right: 1px solid #fff; }

.address-input-wrapper {
  flex: 1;
  background: white;
  border: 1px inset #fff;
  height: 24px;
  padding: 0 4px;
  display: flex; align-items: center;
}
.address-input { font-size: 12px; width: 100%; }

/* 3. 视口与通用 */
.viewport {
  flex: 1;
  background: white;
  border: 2px inset #fff;
  margin: 2px;
  position: relative;
  display: flex; flex-direction: column;
  overflow: hidden;
  height: 0; /* 配合flex:1 确保滚动正常 */
}

.web-content { flex: 1; overflow-y: auto; }

.loading-bar-container { padding: 20px; text-align: center; }
.loading-track { height: 15px; border: 1px inset #fff; background: #fff; margin-top: 5px; }
.loading-progress { height: 100%; background: #000080; transition: width 0.2s; }

/* 底部状态栏 */
.status-bar {
  height: 20px;
  background: #c0c0c0;
  border-top: 1px solid #fff;
  display: flex; align-items: center; padding: 0 5px; font-size: 11px;
  flex-shrink: 0; /* 防止被压缩 */
}
.status-text { flex: 1; }
.status-progress-bar { height: 10px; background: #000080; width: 50px; border: 1px inset #fff; }

/* ============ SCENE A: Portal Styles ============ */
.page-home { padding: 15px; }
.hero-section { text-align: center; margin-bottom: 20px; }
.hero-logo { width: 80px; height: 80px; margin: 10px 0; }
.blink-text { animation: blink 1s infinite; color: red; font-weight: bold; }
@keyframes blink { 50% { opacity: 0; } }
.daily-quote { display: block; font-style: italic; color: #666; font-size: 12px; }
.retro-hr { border-bottom: 1px solid #808080; border-top: 1px solid #fff; margin: 15px 0; height: 0; }

.link-item {
  display: flex; align-items: center; margin-bottom: 15px; padding: 5px;
  border: 1px dashed transparent; cursor: pointer;
}
.link-item:active { background: #ffffcc; border-color: #000; }
.special-link { background: #e0e0ff; border: 1px solid #000080; }
.link-icon { font-size: 24px; margin-right: 10px; }
.link-title { display: block; color: #0000ff; text-decoration: underline; font-weight: bold; }
.link-desc { font-size: 12px; color: #666; }

/* ============ 2006 Portal Styles ============ */
.portal-2006 {
  background: #f0f0f0;
  font-family: 'SimSun', '宋体', Arial, sans-serif;
  font-size: 12px;
  line-height: 1.5;
}

/* 怀旧模拟提示横幅 */
.simulation-banner {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  background: linear-gradient(135deg, rgba(255, 193, 7, 0.25) 0%, rgba(255, 152, 0, 0.2) 100%);
  border: 2px solid rgba(255, 193, 7, 0.6);
  padding: 12rpx 20rpx;
  margin: 10px 15px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(255, 193, 7, 0.3);
}

.banner-icon {
  font-size: 14px;
  animation: pulse 2s ease-in-out infinite;
}

.banner-text {
  font-size: 12px;
  color: #f57c00;
  font-weight: bold;
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.7;
  }
}

/* 顶部跑马灯 */
.top-banner {
  background: linear-gradient(180deg, #ff9933 0%, #ff6600 100%);
  padding: 8px;
  border-bottom: 2px solid #cc5200;
}

.marquee-text {
  color: #fff;
  font-size: 13px;
  white-space: nowrap;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
}

/* Logo 和 搜索区 */
.search-section {
  background: #fff;
  padding: 15px;
  border-bottom: 1px solid #ccc;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.site-logo {
  text-align: center;
  margin-bottom: 15px;
}

.logo-main {
  display: block;
  font-size: 28px;
  font-weight: bold;
  color: #003366;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
}

.logo-sub {
  display: block;
  font-size: 12px;
  color: #666;
  margin-top: 5px;
}

.search-box {
  width: 100%;
  max-width: 500px;
  background: #f5f5f5;
  border: 1px solid #999;
  padding: 10px;
}

.search-tabs {
  display: flex;
  gap: 5px;
  margin-bottom: 8px;
}

.search-tab {
  padding: 4px 12px;
  background: #ddd;
  border: 1px solid #999;
  font-size: 12px;
  cursor: pointer;
}

.search-tab.active {
  background: #fff;
  border-bottom: 2px solid #f60;
  font-weight: bold;
  color: #f60;
}

.search-input-row {
  display: flex;
  gap: 5px;
  margin-bottom: 8px;
}

.search-input {
  flex: 1;
  padding: 6px 8px;
  border: 1px solid #999;
  font-size: 13px;
}

.search-btn {
  padding: 6px 16px;
  background: linear-gradient(180deg, #ff9933 0%, #ff6600 100%);
  border: 1px solid #cc5200;
  color: #fff;
  font-size: 13px;
  font-weight: bold;
  cursor: pointer;
}

.hot-keywords {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.hot-label {
  color: #999;
  font-size: 12px;
}

.hot-keyword {
  color: #0066cc;
  font-size: 12px;
  cursor: pointer;
}

.hot-keyword:hover {
  text-decoration: underline;
}

/* 快捷工具区 - 支持横向滚动 */
.quick-tools {
  display: flex;
  justify-content: flex-start;
  background: #fff;
  padding: 8px 10px;
  border-bottom: 1px solid #ccc;
  flex-wrap: nowrap;
  overflow-x: auto;
  overflow-y: hidden;
  /* 隐藏滚动条 */
  -webkit-overflow-scrolling: touch;
}

.quick-tools::-webkit-scrollbar {
  display: none;
}

.quick-tools .tool-item {
  width: 80px;
  min-width: 80px;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 5px 8px;
  cursor: pointer;
  flex-shrink: 0;
}

.quick-tools .tool-item:active {
  background: #ffffcc;
}

.quick-tools .tool-icon {
  font-size: 20px;
  margin-bottom: 2px;
}

.quick-tools .tool-name {
  font-size: 10px;
  color: #333;
  text-align: center;
}

/* 快捷导航 */
.quick-links {
  display: flex;
  justify-content: space-around;
  background: #fff;
  padding: 10px;
  border-bottom: 1px solid #ccc;
}

.quick-link-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 8px 12px;
  cursor: pointer;
}

.quick-link-item:active {
  background: #ffffcc;
}

.quick-icon {
  font-size: 20px;
  margin-bottom: 4px;
}

.quick-text {
  font-size: 11px;
  color: #333;
}

/* 主内容区 - 单栏布局 */
.main-content {
  padding: 8px;
  background: #e8e8e8;
}

.content-col {
  background: #fff;
  border: 1px solid #999;
}

/* 新闻区 */
.section-header {
  background: linear-gradient(180deg, #4a90d9 0%, #2d5c8a 100%);
  padding: 6px 10px;
  color: #fff;
  font-weight: bold;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.section-header.blue-header {
  background: linear-gradient(180deg, #4a90d9 0%, #2d5c8a 100%);
}

.section-header.red-header {
  background: linear-gradient(180deg, #d94a4a 0%, #8a2d2d 100%);
}

.section-header.green-header {
  background: linear-gradient(180deg, #6bc96b 0%, #3d8a3d 100%);
}

.section-header.orange-header {
  background: linear-gradient(180deg, #f90 0%, #c60 100%);
}

.section-header.purple-header {
  background: linear-gradient(180deg, #9966ff 0%, #6633cc 100%);
}

.section-title {
  font-size: 13px;
}

.more-link {
  font-size: 11px;
  cursor: pointer;
}

.news-section {
  margin-bottom: 10px;
}

.news-list {
  padding: 8px;
}

.news-item {
  padding: 6px 0;
  border-bottom: 1px dotted #ccc;
}

.news-item:last-child {
  border-bottom: none;
}

.news-title {
  font-size: 12px;
  color: #333;
  display: block;
  line-height: 1.6;
}

.news-title.highlight {
  color: #f60;
  font-weight: bold;
}

.news-time {
  font-size: 10px;
  color: #999;
  display: block;
  margin-top: 2px;
}

/* 工具区 */
.tools-section {
  margin-bottom: 10px;
}

.tools-grid {
  display: flex;
  flex-wrap: wrap;
  padding: 10px;
  gap: 10px;
}

.tool-item {
  width: 30%;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 10px;
  background: #f8f8f8;
  border: 1px solid #ddd;
  cursor: pointer;
}

.tool-item:active {
  background: #ffffcc;
}

.tool-icon {
  font-size: 24px;
  margin-bottom: 6px;
}

.tool-name {
  font-size: 11px;
  color: #333;
  text-align: center;
}

/* 右栏 */
.ad-banner {
  background: linear-gradient(180deg, #ffcc00 0%, #ff9900 100%);
  padding: 10px;
  margin: 8px;
  border: 2px solid #f60;
  text-align: center;
}

.ad-content {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.ad-title {
  font-size: 10px;
  color: #999;
}

.ad-text {
  font-size: 18px;
  font-weight: bold;
  color: #f00;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
}

.ad-desc {
  font-size: 12px;
  color: #333;
}

.ad-btn {
  padding: 4px 12px;
  background: #f00;
  color: #fff;
  font-size: 12px;
  border-radius: 3px;
  align-self: center;
}

.side-section {
  margin: 8px;
  border: 1px solid #999;
}

.hot-list {
  padding: 8px;
}

.hot-item {
  display: flex;
  align-items: center;
  padding: 5px 0;
  border-bottom: 1px dotted #ddd;
}

.hot-rank {
  width: 20px;
  height: 20px;
  background: #999;
  color: #fff;
  text-align: center;
  line-height: 20px;
  font-size: 11px;
  font-weight: bold;
  margin-right: 8px;
}

.hot-top1 {
  background: #f00;
}

.hot-top2 {
  background: #f60;
}

.hot-top3 {
  background: #fc0;
}

.hot-name {
  font-size: 12px;
  color: #333;
}

/* 留言区 */
.comment-list {
  padding: 8px;
}

.comment-item {
  padding: 6px 0;
  border-bottom: 1px dotted #ddd;
  font-size: 11px;
}

.comment-user {
  color: #666;
  font-weight: bold;
}

.comment-text {
  color: #999;
}

/* 计数器 */
.counter-box {
  background: #fff;
  padding: 10px;
  margin: 8px;
  text-align: center;
  border: 1px solid #999;
}

.counter-label {
  font-size: 11px;
  color: #666;
}

.counter-number {
  font-size: 16px;
  font-weight: bold;
  color: #f00;
  font-family: 'Courier New', monospace;
}

.bookmark-box {
  background: #ffffcc;
  padding: 8px;
  margin: 8px;
  text-align: center;
  border: 1px dashed #f60;
}

.bookmark-text {
  font-size: 11px;
  color: #f60;
}

/* 底部 */
.portal-footer {
  background: #333;
  color: #ccc;
  padding: 15px;
  text-align: center;
  border-top: 3px solid #666;
}

.footer-links {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 5px;
  margin-bottom: 8px;
}

.footer-link {
  color: #999;
  font-size: 11px;
  cursor: pointer;
}

.footer-link:hover {
  color: #fff;
}

.footer-divider {
  color: #666;
}

.footer-copyright {
  display: block;
  font-size: 11px;
  margin-bottom: 5px;
}

.footer-icp {
  display: block;
  font-size: 10px;
  color: #666;
}

.footer-notice {
  display: block;
  font-size: 9px;
  color: #888;
  margin-top: 3px;
  font-style: italic;
}

/* ============ 工具页面通用样式 ============ */
.tool-page {
  min-height: 100vh;
  background: #f0f0f0;
  font-family: 'SimSun', '宋体', Arial, sans-serif;
  padding-bottom: 30px;
}

.tool-header {
  background: linear-gradient(180deg, #3366cc 0%, #003399 100%);
  padding: 15px;
  text-align: center;
  border-bottom: 3px solid #003366;
}

.tool-title {
  display: block;
  font-size: 18px;
  font-weight: bold;
  color: #fff;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
  margin-bottom: 5px;
}

.tool-subtitle {
  display: block;
  font-size: 12px;
  color: #aaccff;
}

.tool-body {
  background: #fff;
  margin: 10px;
  padding: 15px;
  border: 1px solid #999;
  border-radius: 3px;
}

.tool-footer {
  background: #ffffcc;
  margin: 10px;
  padding: 10px;
  border: 1px dashed #f60;
  text-align: center;
}

.tool-footer text {
  display: block;
  font-size: 11px;
  color: #666;
  margin-bottom: 3px;
}

/* ============ 火星翻译样式 ============ */
.mode-selector {
  display: flex;
  gap: 5px;
  margin-bottom: 15px;
  flex-wrap: wrap;
}

.mode-btn {
  flex: 1;
  min-width: 60px;
  padding: 8px 12px;
  background: #ddd;
  border: 1px solid #999;
  font-size: 13px;
  color: #333;
}

.mode-btn.active {
  background: #0066cc;
  color: #fff;
  border-color: #004499;
  font-weight: bold;
}

.input-area {
  width: 100%;
  min-height: 80px;
  padding: 10px;
  border: 1px solid #999;
  font-size: 14px;
  margin-bottom: 10px;
  box-sizing: border-box;
  background: #fafafa;
}

.convert-btn {
  width: 100%;
  padding: 10px;
  background: linear-gradient(180deg, #ff9933 0%, #ff6600 100%);
  border: 1px solid #cc5200;
  color: #fff;
  font-size: 14px;
  font-weight: bold;
  margin-bottom: 15px;
}

.output-area {
  width: 100%;
  min-height: 80px;
  padding: 10px;
  border: 1px solid #999;
  font-size: 14px;
  margin-bottom: 10px;
  box-sizing: border-box;
  background: #f0f8ff;
  color: #003366;
}

.copy-btn {
  width: 100%;
  padding: 8px;
  background: #666;
  color: #fff;
  font-size: 13px;
}

/* ============ 非主流相机样式 ============ */
.preview-container {
  text-align: center;
  margin-bottom: 15px;
}

.preview-image {
  width: 100%;
  max-height: 250px;
  border: 2px solid #333;
  margin-bottom: 10px;
}

.preview-placeholder {
  width: 100%;
  height: 200px;
  background: #eee;
  border: 2px dashed #999;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #999;
  margin-bottom: 10px;
}

.choose-btn {
  padding: 8px 20px;
  background: linear-gradient(180deg, #6600cc 0%, #4400aa 100%);
  border: 1px solid #330088;
  color: #fff;
  font-size: 13px;
}

.filter-selector,
.border-selector {
  margin-bottom: 15px;
}

.section-title {
  display: block;
  font-size: 12px;
  font-weight: bold;
  color: #333;
  margin-bottom: 8px;
}

.filter-list,
.border-list {
  display: flex;
  flex-wrap: wrap;
  gap: 5px;
}

.filter-btn,
.border-btn {
  padding: 6px 12px;
  background: #eee;
  border: 1px solid #999;
  font-size: 12px;
  color: #333;
}

.filter-btn.active,
.border-btn.active {
  background: #ff69b4;
  color: #fff;
  border-color: #cc3399;
  font-weight: bold;
}

.save-btn {
  width: 100%;
  padding: 10px;
  background: linear-gradient(180deg, #00cc66 0%, #009944 100%);
  border: 1px solid #007733;
  color: #fff;
  font-size: 14px;
  font-weight: bold;
}

/* ============ 星际探索样式 ============ */
.game-status {
  display: block;
  font-size: 12px;
  color: #aaccff;
  margin-top: 5px;
}

.difficulty-selector {
  display: flex;
  gap: 5px;
  margin-bottom: 15px;
  justify-content: center;
}

.diff-btn {
  padding: 6px 15px;
  background: #ddd;
  border: 1px solid #999;
  font-size: 12px;
}

.diff-btn.active {
  background: #ff69b4;
  color: #fff;
  border-color: #cc3399;
  font-weight: bold;
}

.game-grid {
  display: grid;
  gap: 2px;
  margin-bottom: 15px;
  max-height: 300px;
  overflow: auto;
}

.cell {
  aspect-ratio: 1;
  min-height: 28px;
  background: linear-gradient(180deg, #fff 0%, #ddd 100%);
  border: 1px outset #eee;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: bold;
  cursor: pointer;
}

.cell:active {
  border-style: inset;
}

.cell.revealed {
  background: #ccc;
  border-style: inset;
}

.cell.marked {
  background: #ffccaa;
}

.cell.blackhole {
  background: #000;
  color: #fff;
}

.restart-btn {
  width: 100%;
  padding: 8px;
  background: #666;
  color: #fff;
  font-size: 13px;
}

/* ============ 在线计算器样式 ============ */
.calculator-body {
  background: #e0e0e0;
  padding: 8px;
  border: 3px outset #fff;
}

.calc-display {
  background: #9ead86;
  border: 2px inset #fff;
  padding: 8px;
  margin-bottom: 8px;
  text-align: right;
}

.calc-screen {
  font-size: 20px;
  font-family: 'Courier New', monospace;
  color: #000;
  font-weight: bold;
}

.calc-keys {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 3px;
}

.calc-btn {
  padding: 10px 0;
  background: #c0c0c0;
  border: 2px outset #fff;
  font-size: 14px;
  font-weight: bold;
  color: #000;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  user-select: none;
}

.calc-btn:active {
  border-style: inset;
}

.calc-btn.clear {
  background: #ff6666;
}

.calc-btn.equals {
  background: #66cc66;
}

.calc-btn.zero {
  grid-column: span 2;
}

/* ============ 万年历样式 ============ */
.calendar-body {
  padding: 10px;
}

.month-selector {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px;
  background: #f0f0f0;
  border: 1px solid #999;
  margin-bottom: 10px;
}

.current-month {
  font-size: 16px;
  font-weight: bold;
  color: #333;
}

.month-nav {
  padding: 5px 10px;
  background: #ddd;
  border: 1px solid #999;
  font-size: 12px;
}

.weekdays {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 2px;
  margin-bottom: 5px;
}

.weekday {
  text-align: center;
  padding: 8px 0;
  font-size: 12px;
  font-weight: bold;
  color: #666;
  background: #f5f5f5;
}

.calendar-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 2px;
  margin-bottom: 15px;
}

.calendar-day {
  aspect-ratio: 1;
  min-height: 40px;
  padding: 3px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  font-size: 12px;
  background: #fff;
  border: 1px solid #ddd;
}

.calendar-day.other-month {
  background: #f0f0f0;
  color: #ccc;
}

.calendar-day.today {
  background: #ffeb3b;
  border-color: #f60;
  font-weight: bold;
}

.day-number {
  font-size: 14px;
  font-weight: bold;
  color: #333;
}

.lunar-day {
  font-size: 9px;
  color: #999;
  margin-top: 2px;
}

.festival {
  font-size: 9px;
  color: #f60;
  margin-top: 2px;
  font-weight: bold;
}

.today-info {
  background: #fff9e6;
  padding: 12px;
  border: 1px solid #f60;
  border-radius: 5px;
}

.info-title {
  display: block;
  font-size: 14px;
  font-weight: bold;
  color: #333;
  margin-bottom: 5px;
}

.info-lunar {
  display: block;
  font-size: 12px;
  color: #666;
  margin-bottom: 3px;
}

.info-term {
  display: block;
  font-size: 12px;
  color: #f60;
}

/* ==================== Win98 风格弹窗样式 ==================== */
.win98-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  transition: opacity 0.3s;
}

.win98-overlay.hide {
  opacity: 0;
  pointer-events: none;
}

.win98-overlay.show {
  opacity: 1;
}

.win98-window {
  background: #c0c0c0;
  border: 2rpx solid #fff;
  border-right-color: #404040;
  border-bottom-color: #404040;
  box-shadow: 4rpx 4rpx 0 rgba(0, 0, 0, 0.5);
}

.win98-dialog-window {
  width: 500rpx;
  max-width: 90%;
}

.win98-title-bar {
  background: linear-gradient(90deg, #000080 0%, #1084d0 100%);
  padding: 8rpx 10rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.win98-title-text {
  color: #fff;
  font-size: 28rpx;
  font-weight: bold;
}

.win98-title-controls {
  display: flex;
  gap: 10rpx;
}

.win98-btn-close {
  width: 40rpx;
  height: 40rpx;
  padding: 0;
  background: #c0c0c0;
  border: 2rpx solid #fff;
  border-right-color: #404040;
  border-bottom-color: #404040;
  font-size: 32rpx;
  line-height: 36rpx;
  color: #000;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.1s;
}

.win98-btn-close:active {
  border: 2rpx solid #404040;
  border-right-color: #fff;
  border-bottom-color: #fff;
}

.win98-window-body {
  padding: 20rpx;
}

.win98-dialog-content {
  padding: 20rpx;
  min-height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.win98-dialog-text {
  font-size: 26rpx;
  color: #000;
  line-height: 1.8;
  white-space: pre-line;
  text-align: center;
  font-family: 'SimSun', '宋体', sans-serif;
  text-shadow: 1px 1px 0 rgba(255, 255, 255, 0.3);
}

.win98-btn-std {
  background: #c0c0c0;
  border: 2rpx solid #fff;
  border-right-color: #404040;
  border-bottom-color: #404040;
  padding: 15rpx 40rpx;
  font-size: 28rpx;
  color: #000;
  cursor: pointer;
  transition: all 0.1s;
  user-select: none;
  margin: 0 10rpx;
}

.win98-btn-std:active {
  border: 2rpx solid #404040;
  border-right-color: #fff;
  border-bottom-color: #fff;
}

.dialog-actions {
  display: flex;
  justify-content: center;
  margin-top: 20rpx;
}