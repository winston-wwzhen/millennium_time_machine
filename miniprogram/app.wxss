/**app.wxss**/
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  box-sizing: border-box;
} 

button {
  background: initial;
}

button:focus{
  outline: 0;
}

button::after{
  border: none;
}


page {
  background: #f6f6f6;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

/* 动画关键帧：模拟随机的信号撕裂 */
@keyframes glitch-anim-1 {
  0% { clip-path: inset(20% 0 80% 0); transform: translate(-2px, 1px); }
  20% { clip-path: inset(60% 0 10% 0); transform: translate(2px, -1px); }
  40% { clip-path: inset(40% 0 50% 0); transform: translate(-2px, 2px); }
  60% { clip-path: inset(80% 0 5% 0); transform: translate(2px, -2px); }
  80% { clip-path: inset(10% 0 70% 0); transform: translate(-1px, 1px); }
  100% { clip-path: inset(30% 0 50% 0); transform: translate(1px, -1px); }
}

@keyframes glitch-anim-2 {
  0% { clip-path: inset(10% 0 60% 0); transform: translate(2px, -1px); }
  20% { clip-path: inset(80% 0 5% 0); transform: translate(-2px, 2px); }
  40% { clip-path: inset(30% 0 20% 0); transform: translate(2px, 1px); }
  60% { clip-path: inset(10% 0 80% 0); transform: translate(-1px, -2px); }
  80% { clip-path: inset(50% 0 30% 0); transform: translate(1px, 2px); }
  100% { clip-path: inset(20% 0 70% 0); transform: translate(-2px, 1px); }
}

/* 基础故障文字类 */
.glitch-text {
  position: relative;
  display: inline-block;
  color: #000; /* 默认文字颜色，可被具体页面覆盖 */
  font-weight: bold; /* 粗体效果更好 */
}

/* 伪元素通用设置：复制一份文字内容 */
.glitch-text::before,
.glitch-text::after {
  content: attr(data-text); /* 关键：读取 data-text 属性 */
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: inherit; /* 继承背景色，用于遮挡 */
  overflow: hidden;
}

/* 🔴 红/粉色 错位层 */
.glitch-text::before {
  left: -2px;
  text-shadow: 2px 0 #ff00c1; /* 赛博霓虹粉 */
  clip-path: inset(0 0 0 0);
  /* 2秒循环，线性运动，反向交替 */
  animation: glitch-anim-1 2.5s infinite linear alternate-reverse;
}

/* 🔵 蓝/青色 错位层 */
.glitch-text::after {
  left: 2px;
  text-shadow: -2px 0 #00fff9; /* 赛博霓虹蓝 */
  clip-path: inset(0 0 0 0);
  /* 3秒循环，错开节奏 */
  animation: glitch-anim-2 3s infinite linear alternate-reverse;
}

/* 如果需要在点击态（Hover）才触发，可以用这个类 */
.glitch-hover:active .glitch-text::before,
.glitch-hover:active .glitch-text::after {
  /* 这里可以写针对 active 状态的特定逻辑，或者默认静止，active 动 */
  animation-play-state: running;
}