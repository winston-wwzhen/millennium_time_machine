<!-- 彩蛋发现弹窗 - Win98 风格 -->
<!-- 移除 overlay 的 bindtap，只允许通过按钮关闭，确保用户不会误触关闭 -->
<view class="win98-overlay {{show ? 'show' : 'hide'}}" wx:if="{{show}}">
  <view class="win98-window egg-discovery-dialog" catchtap="stopPropagation" catchlongpress="stopPropagation" catchtouchmove="stopPropagation">
    <view class="win98-title-bar">
      <view class="win98-title-text">🎉 发现彩蛋！</view>
      <view class="win98-title-controls">
        <view class="win98-btn-close" bindtap="onClose">×</view>
      </view>
    </view>
    <view class="win98-window-body">
      <view class="egg-discovery-content">
        <view class="egg-icon-wrapper">
          <text class="egg-icon-emoji">🥚</text>
        </view>
        <view class="egg-discovery-info">
          <text class="egg-name">{{eggName}}</text>
          <text class="egg-description">"{{eggDescription}}"</text>
          <view class="egg-rarity-badge rarity-{{eggRarity}}">
            <text class="rarity-text">{{eggRarityName}}</text>
          </view>
          <text class="egg-reward">奖励: {{eggRewardText}}</text>
        </view>
      </view>
      <view class="dialog-actions">
        <view class="win98-btn-std" bindtap="onClose">太棒了！</view>
      </view>
    </view>
  </view>
</view>
