# 后台管理系统设计

> 小程序数据管理、分析、运营的可视化后台
>
> 讨论日期：2025-12-26

---

## 一、系统概述

### 核心功能

```
数据看板 → 用户分析 → 内容管理 → 运营工具 → 系统设置
```

### 设计目标

| 目标 | 说明 |
|------|------|
| **可视化** | 直观展示各类数据和图表 |
| **便捷性** | 简单易用，无需技术背景 |
| **实时性** | 数据实时更新 |
| **安全性** | 权限控制，保护数据 |
| **低成本** | 利用现有云开发资源 |

---

## 二、技术方案

### 方案对比

| 方案 | 优势 | 劣势 | 成本 | 推荐 |
|------|------|------|------|------|
| **云开发+静态页面** | 无需服务器，与小程序共用数据库 | 功能相对简单 | 低 | ✅ |
| **独立服务器+自建** | 功能完整，自由度高 | 需要服务器，成本高 | 中 | |
| **SaaS后台服务** | 快速搭建，功能丰富 | 数据在第三方，有安全风险 | 中高 | |

### 推荐方案：云开发+Vue管理后台

```
技术栈：
• 前端：Vue 3 + Element Plus / Ant Design Vue
• 部署：云开发静态网站托管
• 数据：直接访问云开发数据库
• 认证：云开发自定义登录
```

---

## 三、功能模块设计

### 1. 数据看板（首页）

```
┌─────────────────────────────────────────────────────────┐
│                    千禧时光机 - 数据看板                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐          │
│  │ 总用户数  │  │ 今日新增  │  │ 日活用户  │          │
│  │  12,345  │  │   +123   │  │  1,234   │          │
│  │  ↑ 15%   │  │  ↑ 8%    │  │  ↑ 12%   │          │
│  └───────────┘  └───────────┘  └───────────┘          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │            用户增长趋势（近7天）                  │  │
│  │  ▁▃▅▇▅▃▁  (折线图)                               │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────┐  ┌───────────────┐                │
│  │  游戏完成次数  │  │  分享次数     │                │
│  │    456       │  │    789       │                │
│  └───────────────┘  └───────────────┘                │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │            结局达成分布（饼图）                   │  │
│  │  • 互联网大厂CEO 25%                             │  │
│  │  • 平凡而幸福 20%                                │  │
│  │  • 自由职业者 15%                                │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │            实时活动日志                          │  │
│  │  14:30  用户***86 完成了游戏                      │  │
│  │  14:29  用户***52 分享了结局                      │  │
│  │  14:28  用户***33 发现了彩蛋                      │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2. 用户管理

```
┌─────────────────────────────────────────────────────────┐
│  用户管理                                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  搜索: [______] ▼ 搜索类型       筛选: [全部状态 ▼]     │
│                                                         │
│  ┌────┬─────────┬──────┬──────┬──────┬────────┬──────┐ │
│  │ ID │ 昵称    │ 等级 │ Q点  │ 注册  │ 最后活跃│ 操作 │ │
│  ├────┼─────────┼──────┼──────┼──────┼────────┼──────┤ │
│  │ 1  │ ***86   │ 28   │ 5230 │ 01-10│ 2分钟前│ 详情 │ │
│  │ 2  │ ***52   │ 25   │ 4100 │ 01-08│ 1小时前│ 详情 │ │
│  │ 3  │ ***33   │ 24   │ 3800 │ 01-05│ 昨天   │ 详情 │ │
│  └────┴─────────┴──────┴──────┴──────┴────────┴──────┘ │
│                                                         │
│  共 12,345 条记录    [< 1] 2 3 4 5 [>]                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**用户详情**：
```
┌─────────────────────────────────────────────────────────┐
│  用户详情 - 用户***86                                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  基本信息：                                              │
│  • 等级：LV.28                                          │
│  • Q点：5,230                                           │
│  • 注册时间：2025-01-10 14:30                           │
│  • 最后活跃：2分钟前                                    │
│  • 累计在线：12小时30分                                 │
│                                                         │
│  游戏数据：                                              │
│  • 完成次数：15次                                       │
│  • 达成结局：5种                                        │
│  • 最常结局：互联网大厂CEO (8次)                        │
│                                                         │
│  QCIO数据：                                              │
│  • AI好友：45个                                         │
│  • 聊天次数：1,234次                                    │
│  • 签名："那些年..."                                    │
│                                                         │
│  彩蛋发现：                                              │
│  • 已发现：75/80个                                      │
│  • 最近发现：舞动的小狮子？（2小时前）                  │
│                                                         │
│  [返回] [编辑用户] [查看日志]                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3. 游戏数据分析

```
┌─────────────────────────────────────────────────────────┐
│  游戏数据分析                                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  【整体统计】                                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 总完成数 │  │ 今日完成 │  │ 平均时长 │             │
│  │ 5,678   │  │  456    │  │ 8.5分钟  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                                         │
│  【结局排行榜】                                         │
│  ┌────┬──────────────────┬────────┬──────────┐        │
│  │排名│ 结局名称          │ 达成数 │ 占比     │        │
│  ├────┼──────────────────┼────────┼──────────┤        │
│  │ 1  │ 互联网大厂CEO    │ 1,234  │ 25%      │        │
│  │ 2  │ 平凡而幸福       │  987   │ 20%      │        │
│  │ 3  │ 自由职业者       │  765   │ 15%      │        │
│  └────┴──────────────────┴────────┴──────────┘        │
│                                                         │
│  【结局达成趋势图】                                     │
│  │ 互联网大厂CEO   ▅▅▅▅▅▅▅                            │
│  │ 平凡而幸福     ▃▃▃▅▅▃▃▃                            │
│  │ 自由职业者     ▂▂▃▃▃▂▂▂                            │
│  └─────────────────────────────────────────────────    │
│                                                         │
│  【选择分析】                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │         科技 vs 财富 选择分布                    │   │
│  │  科技60% ████████████░░░░░░░░░                   │   │
│  │  财富40% ████████░░░░░░░░░░░░░░                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4. 排行榜管理

```
┌─────────────────────────────────────────────────────────┐
│  排行榜管理                                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [等级排行] [Q点排行] [彩蛋发现] [结局达成]             │
│                                                         │
│  等级排行榜 TOP100                                      │
│  ┌────┬─────────┬──────┬──────────┬────────┬──────────┐│
│  │排名│ 昵称    │ 等级 │ Q点      │ 达成结局│ 操作     ││
│  ├────┼─────────┼──────┼──────────┼────────┼──────────┤│
│  │ 1  │ ***86   │ 28   │ 5,230    │ 15     │ 详情     ││
│  │ 2  │ ***52   │ 25   │ 4,100    │ 12     │ 详情     ││
│  │ 3  │ ***33   │ 24   │ 3,800    │ 10     │ 详情     ││
│  └────┴─────────┴──────┴──────────┴────────┴──────────┘│
│                                                         │
│  [导出CSV] [导出Excel] [发布到公众号]                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 5. 内容管理

```
┌─────────────────────────────────────────────────────────┐
│  内容管理                                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [AI对话] [用户签名] [彩蛋发现] [分享日志]             │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  AI对话精选                                     │   │
│  ├─────────────────────────────────────────────────┤   │
│  │  • "这个AI网友太搞笑了！" - 1,234次分享         │   │
│  │    [查看] [设为精选] [发布]                     │   │
│  │                                                 │   │
│  │  • "和暗恋对象的对话太真实了" - 987次分享       │   │
│  │    [查看] [设为精选] [发布]                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [添加精选内容] [批量管理]                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6. CDK管理

```
┌─────────────────────────────────────────────────────────┐
│  CDK管理                                                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [生成CDK] [CDK列表] [使用统计]                        │
│                                                         │
│  生成CDK                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  CDK类型：[应用激活 ▼]                           │   │
│  │  应用：[如果当时.exe ▼]                         │   │
│  │  数量：[100]                                     │   │
│  │  备注：[首批预售________]                        │   │
│  │                                                 │   │
│  │  [生成]                                         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  CDK列表                                               │
│  ┌─────────────────┬──────┬──────┬────────┬──────────┐│
│  │ CDK码           │ 类型 │ 状态 │ 使用者  │ 操作     ││
│  ├─────────────────┼──────┼──────┼────────┼──────────┤│
│  │ IFTH-2025-AB... │ 应用 │ 未用 │ -       │ 删除     ││
│  │ IFTH-2025-CD... │ 应用 │ 已激 │ ***52   │ 详情     ││
│  └─────────────────┴──────┴──────┴────────┴──────────┘│
│                                                         │
│  统计：总生成500 | 已使用328 | 未使用172                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 7. 内容创作中心（核心功能）

```
┌─────────────────────────────────────────────────────────┐
│  内容创作中心                                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [排行榜海报] [AI文案生成] [历史发布] [定时发布]       │
│                                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  📊 排行榜海报生成                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  排行榜类型：[等级排行 ▼]                               │
│  展示数量：[TOP10 ▼]                                    │
│  海报风格：[怀旧风 ▼]                                   │
│  日期：[2025-01-15]                                     │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  预览区域                                        │   │
│  │  ┌─────────────────────────────────────────┐    │   │
│  │  │   🏆 千禧时光机 · 等级排行榜            │    │   │
│  │  │   2025-01-15                             │    │   │
│  │  │   🥇 LV.28  用户***86                   │    │   │
│  │  │   🥈 LV.25  用户***52                   │    │   │
│  │  │   🥉 LV.24  用户***33                   │    │   │
│  │  │   ...                                    │    │   │
│  │  │   你的排名？扫码查看 →                  │    │   │
│  │  │   [小程序码]                            │    │   │
│  │  └─────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [生成海报] [预览] [下载图片] [复制到剪贴板]            │
│                                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  ✨ AI文案生成                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                         │
│  目标平台：[小红书 ▼]                                   │
│  内容类型：[排行榜 ▼]                                   │
│  语气风格：[活泼 ▼]                                     │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  生成文案                                        │   │
│  ├─────────────────────────────────────────────────┤   │
│  │  标题：                                          │   │
│  │  千禧时光机排行榜来了！你排第几？🤔             │   │
│  │                                                 │   │
│  │  正文：                                          │   │
│  │  家人们！今天的千禧时光机排行榜出炉啦！🎉      │   │
│  │                                                 │   │
│  │  🏆 等级最高的大神已经LV.28了！                 │   │
│  │  📜 最热的结局是「互联网大厂CEO」               │   │
│  │  🥚 彩蛋大师已经发现了75个彩蛋！                │   │
│  │                                                 │   │
│  │  你发现了几个彩蛋？快来评论区晒一晒！💬         │   │
│  │                                                 │   │
│  │  #千禧时光机 #排行榜 #非主流 #怀旧 #童年记忆    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [重新生成] [复制文案] [复制文案+图片] [保存为模板]     │
│                                                         │
│  💡 提示：可以修改文案后再生成，AI会记住你的偏好       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 8. 运营工具

```
┌─────────────────────────────────────────────────────────┐
│  运营工具                                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [发送通知] [数据导出] [活动管理] [CDK管理]            │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  发送小程序通知                                 │   │
│  ├─────────────────────────────────────────────────┤   │
│  │  目标用户：[全部用户 ▼]                         │   │
│  │  通知标题：[新功能上线啦！________]              │   │
│  │  通知内容：[_______________]                    │   │
│  │  页面路径：[/pages/xxx/__________]              │   │
│  │                                                 │   │
│  │  [发送] (每天限1次，慎重使用)                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 9. 系统设置

```
┌─────────────────────────────────────────────────────────┐
│  系统设置                                               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [基本设置] [权限管理] [日志查看] [API密钥]            │
│                                                         │
│  基本设置                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │  小程序名称：千禧时光机                         │   │
│  │  小程序ID：wx***123                             │   │
│  │  云环境ID：millennium-xxx                       │   │
│  │                                                 │   │
│  │  数据更新频率：[实时 ▼]                         │   │
│  │  排行榜更新：[每小时 ▼]                         │   │
│  │                                                 │   │
│  │  [保存设置]                                     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  管理员账户                                             │
│  ┌─────────────────┬──────────┬────────┬──────────┐   │
│  │ 用户名          │ 角色     │ 状态   │ 操作     │   │
│  ├─────────────────┼──────────┼────────┼──────────┤   │
│  │ admin           │ 超级管理员│ 启用   │ 编辑     │   │
│  │ operator        │ 运营人员  │ 启用   │ 编辑     │   │
│  └─────────────────┴──────────┴────────┴──────────┘   │
│                                                         │
│  [添加管理员]                                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 四、技术实现

### 项目结构

```
admin-panel/
├── src/
│   ├── api/              # API接口
│   │   ├── index.js      # 云开发初始化
│   │   ├── user.js       # 用户相关API
│   │   ├── stats.js      # 统计相关API
│   │   └── cdk.js        # CDK相关API
│   ├── views/            # 页面组件
│   │   ├── Dashboard.vue # 数据看板
│   │   ├── Users.vue     # 用户管理
│   │   ├── Game.vue      # 游戏分析
│   │   ├── Rankings.vue  # 排行榜
│   │   ├── Content.vue   # 内容管理
│   │   ├── CDK.vue       # CDK管理
│   │   ├── Tools.vue     # 运营工具
│   │   └── Settings.vue  # 系统设置
│   ├── components/       # 公共组件
│   ├── router/           # 路由配置
│   └── store/            # 状态管理
├── public/
└── package.json
```

### 云开发接入

```javascript
// src/api/index.js
import cloudbase from '@cloudbase/js-sdk'

const app = cloudbase.init({
  env: 'millennium-xxx' // 云开发环境ID
})

// 自定义登录（需要云函数支持）
async function login(username, password) {
  const res = await app.callFunction({
    name: 'admin-login',
    data: { username, password }
  })
  return res.result
}

// 数据库查询
export async function getUsers(limit = 100) {
  const db = app.database()
  const res = await db.collection('users')
    .orderBy('level', 'desc')
    .limit(limit)
    .get()
  return res.data
}

// 统计数据
export async function getStats() {
  const res = await app.callFunction({
    name: 'admin-stats',
    data: { type: 'all' }
  })
  return res.result
}
```

### 云函数：管理员登录

```javascript
// cloudfunctions/admin-login/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

const db = cloud.database()

exports.main = async (event, context) => {
  const { username, password } = event

  // 查询管理员
  const admin = await db.collection('admins')
    .where({ username })
    .get()

  if (admin.data.length === 0) {
    return { code: -1, message: '管理员不存在' }
  }

  // 验证密码（实际应该用加密比对）
  if (admin.data[0].password !== password) {
    return { code: -1, message: '密码错误' }
  }

  // 生成自定义登录票据
  const ticket = await cloud.getWsToken()

  // 保存登录记录
  await db.collection('admin_logs').add({
    data: {
      adminId: admin.data[0]._id,
      action: 'login',
      timestamp: new Date()
    }
  })

  return {
    code: 0,
    data: {
      token: ticket,
      userInfo: {
        username: admin.data[0].username,
        role: admin.data[0].role
      }
    }
  }
}
```

### 云函数：管理员统计

```javascript
// cloudfunctions/admin-stats/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

const db = cloud.database()
const _ = db.command

exports.main = async (event, context) => {
  const { type } = event

  // 验证权限（应有中间件处理）

  const stats = {}

  // 用户统计
  if (type === 'all' || type === 'user') {
    const totalUsers = await db.collection('users').count()
    const todayNew = await db.collection('users')
      .where({
        createdAt: _.gte(new Date().setHours(0, 0, 0, 0))
      })
      .count()

    stats.user = {
      total: totalUsers.total,
      todayNew: todayNew.total
    }
  }

  // 游戏统计
  if (type === 'all' || type === 'game') {
    const gameResults = await db.collection('game_results')
      .count()

    stats.game = {
      totalResults: gameResults.total
    }
  }

  // 排行榜
  if (type === 'all' || type === 'rank') {
    const levelRank = await db.collection('users')
      .orderBy('level', 'desc')
      .limit(100)
      .field({
        openid: true,
        level: true,
        nickname: true,
        qpoints: true
      })
      .get()

    stats.rank = {
      level: levelRank.data
    }
  }

  return { code: 0, data: stats }
}
```

### 云函数：生成排行榜海报

```javascript
// cloudfunctions/generate-poster/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

const db = cloud.database()

// 使用 puppeteer 生成图片（需要云函数支持）
// 或使用 canvas / html-to-image 等库

exports.main = async (event, context) => {
  const { type, rankType, topN, style } = event

  // 获取排行榜数据
  let rankData
  if (rankType === 'level') {
    rankData = await db.collection('users')
      .orderBy('level', 'desc')
      .limit(topN)
      .field({
        nickname: true,
        level: true,
        qpoints: true
      })
      .get()
  }

  // 生成海报HTML
  const html = generatePosterHTML(rankData.data, style)

  // 转换为图片（使用云函数内的转换服务）
  const imageUrl = await convertHTMLToImage(html)

  // 上传到云存储
  const uploadRes = await cloud.uploadFile({
    cloudPath: `posters/${rankType}_${Date.now()}.png`,
    fileContent: imageUrl
  })

  return {
    code: 0,
    data: {
      imageUrl: uploadRes.fileID,
      tempUrl: await cloud.getTempFileURL({
        fileList: [uploadRes.fileID]
      })
    }
  }
}

function generatePosterHTML(data, style) {
  return `
    <div style="width: 500px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 30px; font-family: Arial;">
      <div style="text-align: center; color: white; margin-bottom: 20px;">
        <h1 style="margin: 0;">🏆 千禧时光机 · 等级排行榜</h1>
        <p style="margin: 5px 0;">${new Date().toLocaleDateString()}</p>
      </div>

      ${data.map((item, index) => `
        <div style="background: rgba(255,255,255,0.9); margin: 10px 0; padding: 15px; border-radius: 10px; display: flex; align-items: center;">
          <div style="font-size: 24px; margin-right: 15px;">
            ${index === 0 ? '🥇' : index === 1 ? '🥈' : index === 2 ? '🥉' : index + 1}
          </div>
          <div style="flex: 1;">
            <div style="font-size: 18px; font-weight: bold;">${maskNickname(item.nickname)}</div>
            <div style="font-size: 14px; color: #666;">LV.${item.level}</div>
          </div>
        </div>
      `).join('')}

      <div style="text-align: center; margin-top: 20px; padding: 15px; background: rgba(255,255,255,0.9); border-radius: 10px;">
        <p style="margin: 0; font-size: 14px;">你的排名？扫码查看 →</p>
        <img src="https://xxx.com/qrcode.png" style="width: 150px; margin-top: 10px;">
      </div>
    </div>
  `
}

function maskNickname(nickname) {
  return nickname.substring(0, 3) + '***'
}
```

### 云函数：AI文案生成

```javascript
// cloudfunctions/ai-copywriter/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

// 接入 AI API（文心一言、通义千问、ChatGPT等）
const axios = require('axios')

exports.main = async (event, context) => {
  const { platform, contentType, tone, statsData } = event

  // 构建AI提示词
  const prompt = buildPrompt(platform, contentType, tone, statsData)

  // 调用AI API
  const aiResponse = await callAIAPI(prompt)

  return {
    code: 0,
    data: {
      title: aiResponse.title,
      content: aiResponse.content,
      hashtags: aiResponse.hashtags,
      raw: aiResponse
    }
  }
}

function buildPrompt(platform, contentType, tone, stats) {
  // 根据不同平台构建不同的提示词
  const platformStyles = {
    xiaohongshu: {
      style: '小红书风格',
      format: '标题+emoji+正文+话题标签',
      tone: '活泼、亲切、像和朋友聊天'
    },
    wechat: {
      style: '公众号风格',
      format: '正式标题+数据统计+分析+结尾',
      tone: '专业、数据驱动'
    },
    weibo: {
      style: '微博风格',
      format: '短小精悍+话题标签+emoji',
      tone: '轻松、有传播性'
    }
  }

  const style = platformStyles[platform]

  return `
你是一个专业的新媒体运营文案生成助手。请根据以下数据生成一篇${style.style}的推广文案。

【数据信息】
- 总用户数：${stats.totalUsers}
- 今日新增：${stats.todayNew}
- 日活用户：${stats.todayActive}
- 等级第一：LV.${stats.topLevel}
- 最热结局：${stats.hotEnding}
- 彩蛋发现最多：${stats.maxEggs}个

【要求】
1. 风格：${style.tone}
2. 格式：${style.format}
3. 语调：${tone}
4. 内容类型：${contentType}
5. 必须包含3-5个相关话题标签

请直接返回文案，不要有多余解释。

【返回格式】
标题：...
正文：...
话题标签：...
`
}

async function callAIAPI(prompt) {
  // 示例：调用通义千问 API
  try {
    const response = await axios.post(
      'https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation',
      {
        model: 'qwen-turbo',
        input: {
          messages: [
            { role: 'system', content: '你是一个专业的新媒体运营文案生成助手。' },
            { role: 'user', content: prompt }
          ]
        }
      },
      {
        headers: {
          'Authorization': `Bearer ${process.env.AI_API_KEY}`,
          'Content-Type': 'application/json'
        }
      }
    )

    const text = response.data.output.text

    // 解析AI返回的内容
    return parseAIResponse(text)
  } catch (error) {
    console.error('AI API调用失败:', error)
    // 返回默认模板
    return getDefaultCopy()
  }
}

function parseAIResponse(text) {
  // 解析AI返回的结构化内容
  const titleMatch = text.match(/标题：(.+?)(?=\n|$)/)
  const contentMatch = text.match(/正文：(.+?)(?=话题标签：|$)/s)
  const tagsMatch = text.match(/话题标签：(.+)/)

  return {
    title: titleMatch ? titleMatch[1].trim() : '千禧时光机每日数据',
    content: contentMatch ? contentMatch[1].trim() : text,
    hashtags: tagsMatch ? tagsMatch[1].trim() : '#千禧时光机 #怀旧'
  }
}

function getDefaultCopy() {
  return {
    title: '千禧时光机排行榜来了！',
    content: '今天的排行榜出炉啦！快来围观~',
    hashtags: '#千禧时光机 #排行榜 #怀旧'
  }
}
```

### 前端组件：内容创作中心

```vue
<!-- src/views/ContentCreator.vue -->
<template>
  <div class="content-creator">
    <!-- 排行榜海报生成 -->
    <el-card class="section">
      <template #header>
        <span>📊 排行榜海报生成</span>
      </template>

      <el-form :model="posterForm" inline>
        <el-form-item label="排行榜类型">
          <el-select v-model="posterForm.rankType">
            <el-option label="等级排行" value="level" />
            <el-option label="Q点排行" value="qpoints" />
            <el-option label="彩蛋发现" value="eggs" />
          </el-select>
        </el-form-item>

        <el-form-item label="展示数量">
          <el-select v-model="posterForm.topN">
            <el-option label="TOP10" :value="10" />
            <el-option label="TOP20" :value="20" />
            <el-option label="TOP50" :value="50" />
          </el-select>
        </el-form-item>

        <el-form-item label="海报风格">
          <el-select v-model="posterForm.style">
            <el-option label="怀旧风" value="retro" />
            <el-option label="科技风" value="tech" />
            <el-option label="简约风" value="minimal" />
          </el-select>
        </el-form-item>

        <el-button type="primary" @click="generatePoster" :loading="generating">
          生成海报
        </el-button>
      </el-form>

      <!-- 预览区 -->
      <div v-if="posterUrl" class="preview-area">
        <el-image :src="posterUrl" fit="contain" style="max-width: 400px;" />
        <div class="action-buttons">
          <el-button @click="downloadPoster">下载图片</el-button>
          <el-button @click="copyPoster">复制到剪贴板</el-button>
        </div>
      </div>
    </el-card>

    <!-- AI文案生成 -->
    <el-card class="section">
      <template #header>
        <span>✨ AI文案生成</span>
      </template>

      <el-form :model="copyForm" inline>
        <el-form-item label="目标平台">
          <el-select v-model="copyForm.platform">
            <el-option label="小红书" value="xiaohongshu" />
            <el-option label="公众号" value="wechat" />
            <el-option label="微博" value="weibo" />
          </el-select>
        </el-form-item>

        <el-form-item label="内容类型">
          <el-select v-model="copyForm.contentType">
            <el-option label="排行榜" value="rank" />
            <el-option label="数据报告" value="stats" />
            <el-option label="活动通知" value="event" />
          </el-select>
        </el-form-item>

        <el-form-item label="语气风格">
          <el-select v-model="copyForm.tone">
            <el-option label="活泼" value="lively" />
            <el-option label="专业" value="professional" />
            <el-option label="幽默" value="humor" />
          </el-select>
        </el-form-item>

        <el-button type="primary" @click="generateCopy" :loading="generatingCopy">
          生成文案
        </el-button>
      </el-form>

      <!-- 文案编辑区 -->
      <div v-if="generatedCopy" class="copy-editor">
        <el-input v-model="generatedCopy.title" placeholder="标题" />
        <el-input
          v-model="generatedCopy.content"
          type="textarea"
          :rows="6"
          placeholder="正文"
          style="margin-top: 10px;"
        />
        <el-input
          v-model="generatedCopy.hashtags"
          placeholder="话题标签"
          style="margin-top: 10px;"
        />

        <div class="action-buttons">
          <el-button @click="copyText('copy')">复制文案</el-button>
          <el-button @click="copyText('all')">复制文案+图片</el-button>
          <el-button @click="saveTemplate">保存为模板</el-button>
        </div>
      </div>
    </el-card>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import { ElMessage } from 'element-plus'

const posterForm = reactive({
  rankType: 'level',
  topN: 10,
  style: 'retro'
})

const copyForm = reactive({
  platform: 'xiaohongshu',
  contentType: 'rank',
  tone: 'lively'
})

const posterUrl = ref('')
const generating = ref(false)
const generatingCopy = ref(false)
const generatedCopy = ref(null)

// 生成海报
async function generatePoster() {
  generating.value = true
  try {
    const res = await wx.cloud.callFunction({
      name: 'generate-poster',
      data: posterForm
    })

    posterUrl.value = res.result.data.tempUrl
    ElMessage.success('海报生成成功！')
  } catch (error) {
    ElMessage.error('生成失败：' + error.message)
  } finally {
    generating.value = false
  }
}

// 生成文案
async function generateCopy() {
  generatingCopy.value = true
  try {
    // 先获取统计数据
    const statsRes = await wx.cloud.callFunction({
      name: 'admin-stats',
      data: { type: 'all' }
    })

    // 调用AI文案生成
    const res = await wx.cloud.callFunction({
      name: 'ai-copywriter',
      data: {
        ...copyForm,
        statsData: statsRes.result.data
      }
    })

    generatedCopy.value = res.result.data
    ElMessage.success('文案生成成功！')
  } catch (error) {
    ElMessage.error('生成失败：' + error.message)
  } finally {
    generatingCopy.value = false
  }
}

// 复制到剪贴板
function copyText(type) {
  let text = ''
  if (type === 'copy') {
    text = `${generatedCopy.value.title}\n\n${generatedCopy.value.content}\n\n${generatedCopy.value.hashtags}`
  } else if (type === 'all') {
    text = `${generatedCopy.value.title}\n\n${generatedCopy.value.content}\n\n${generatedCopy.value.hashtags}\n\n[图片：${posterUrl.value}]`
  }

  navigator.clipboard.writeText(text)
  ElMessage.success('已复制到剪贴板！')
}

// 下载海报
function downloadPoster() {
  const link = document.createElement('a')
  link.href = posterUrl.value
  link.download = `排行榜_${Date.now()}.png`
  link.click()
}

// 保存模板
function saveTemplate() {
  // 保存到本地存储或云数据库
  localStorage.setItem('copyTemplate', JSON.stringify(generatedCopy.value))
  ElMessage.success('模板已保存！')
}
</script>

<style scoped>
.content-creator {
  padding: 20px;
}

.section {
  margin-bottom: 20px;
}

.preview-area {
  margin-top: 20px;
  text-align: center;
}

.copy-editor {
  margin-top: 20px;
  padding: 15px;
  background: #f5f5f5;
  border-radius: 8px;
}

.action-buttons {
  margin-top: 15px;
  display: flex;
  gap: 10px;
}
</style>
```

---

## 五、部署方案

### 云开发静态托管

```bash
# 1. 构建项目
npm run build

# 2. 安装云开发CLI
npm install -g @cloudbase/cli

# 3. 登录
cloudbase login

# 4. 初始化
cloudbase init

# 5. 部署
cloudbase hosting deploy
```

### 配置文件

```json
{
  "envId": "millennium-xxx",
  "region": "ap-shanghai",
  "hosting": {
    "publicPath": "/",
    "ignore": []
  }
}
```

### 访问配置

1. **开发阶段**：使用云开发提供的临时域名
2. **生产阶段**：
   - 方案A：绑定自己的域名（需备案）
   - 方案B：通过云函数代理访问

---

## 六、权限与安全

### 权限设计

| 角色 | 权限 |
|------|------|
| **超级管理员** | 全部权限 |
| **运营人员** | 查看数据、内容管理、运营工具 |
| **只读用户** | 仅查看数据 |

### 安全措施

| 措施 | 说明 |
|------|------|
| **登录验证** | 用户名+密码 |
| **云函数鉴权** | 验证管理员身份 |
| **数据脱敏** | 敏感信息隐藏 |
| **操作日志** | 记录所有操作 |
| **IP白名单** | 限制访问来源（可选） |

---

## 七、开发优先级

### 第一期：基础框架

| 功能 | 优先级 | 时间 |
|------|--------|------|
| 项目搭建 | 🔴 高 | 1天 |
| 登录系统 | 🔴 高 | 1天 |
| 数据看板 | 🔴 高 | 2天 |
| 用户管理 | 🔴 高 | 2天 |

### 第二期：核心运营功能（重点）

| 功能 | 优先级 | 时间 |
|------|--------|------|
| **内容创作中心** | 🔴 高 | 3天 |
| ├─ 排行榜海报生成 | 🔴 高 | 1.5天 |
| ├─ AI文案生成 | 🔴 高 | 1.5天 |
| 游戏数据分析 | 🟡 中 | 2天 |
| 排行榜管理 | 🟡 中 | 1天 |
| CDK管理 | 🟡 中 | 2天 |

### 第三期：增强功能

| 功能 | 优先级 | 时间 |
|------|--------|------|
| 内容管理 | 🟢 低 | 2天 |
| 数据导出 | 🟢 低 | 1天 |
| 通知发送 | 🟢 低 | 1天 |
| 系统设置 | 🟢 低 | 1天 |

### AI服务接入说明

| AI服务 | 优势 | 成本 | 推荐 |
|--------|------|------|------|
| **GLM** | 智谱AI，中文效果优秀，API稳定 | 新用户免费额度 | ✅ |
| **通义千问** | 阿里云，稳定，中文好 | 新用户免费 | ✅ |
| 文心一言 | 百度，功能多 | 新用户免费 | ✅ |
| ChatGPT | 效果最好 | 需海外 | |
| 讯飞星火 | 科大讯飞，中文好 | 新用户免费 | |

**建议**：优先使用GLM，中文效果好且稳定，免费额度充足。

---

## 八、数据可视化建议

### 图表库选择

| 库 | 优势 | 劣势 | 推荐 |
|----|------|------|------|
| **ECharts** | 功能强大，中文文档 | 体积大 | ✅ |
| Chart.js | 轻量，易用 | 功能相对少 | |
| D3.js | 自由度高 | 学习成本高 | |

### 可视化示例

```vue
<template>
  <div class="chart-container">
    <ECharts :option="chartOption" />
  </div>
</template>

<script>
export default {
  data() {
    return {
      chartOption: {
        title: { text: '用户增长趋势' },
        xAxis: {
          type: 'category',
          data: ['周一', '周二', '周三', '周四', '周五', '周六', '周日']
        },
        yAxis: { type: 'value' },
        series: [{
          data: [120, 200, 150, 180, 220, 280, 250],
          type: 'line',
          smooth: true
        }]
      }
    }
  }
}
</script>
```

---

## 九、海报生成技术方案

### 方案对比

| 方案 | 优势 | 劣势 | 成本 | 推荐 |
|------|------|------|------|------|
| **Canvas API** | 云函数原生支持，稳定 | 样式定制较复杂 | 低 | ✅ |
| **Puppeteer** | 效果最好，支持复杂样式 | 云函数需要特殊配置 | 中 | |
| **第三方服务** | 接入简单，效果可控 | 有调用费用，依赖外部 | 中 | |

### 推荐方案：Canvas API

```javascript
// 使用 canvas 库（node-canvas）
const { createCanvas, loadImage } = require('canvas')

async function generateRankingPoster(rankData, style) {
  // 创建画布
  const canvas = createCanvas(500, 800)
  const ctx = canvas.getContext('2d')

  // 背景
  const gradient = ctx.createLinearGradient(0, 0, 500, 800)
  gradient.addColorStop(0, '#667eea')
  gradient.addColorStop(1, '#764ba2')
  ctx.fillStyle = gradient
  ctx.fillRect(0, 0, 500, 800)

  // 标题
  ctx.fillStyle = '#ffffff'
  ctx.font = 'bold 32px Arial'
  ctx.textAlign = 'center'
  ctx.fillText('🏆 千禧时光机 · 等级排行榜', 250, 60)

  // 日期
  ctx.font = '16px Arial'
  ctx.fillText(new Date().toLocaleDateString(), 250, 90)

  // 排行榜内容
  let y = 140
  rankData.forEach((item, index) => {
    // 背景
    ctx.fillStyle = 'rgba(255, 255, 255, 0.9)'
    roundRect(ctx, 30, y, 440, 60, 10)
    ctx.fill()

    // 排名
    ctx.font = '24px Arial'
    ctx.fillStyle = '#333333'
    ctx.textAlign = 'left'
    const medal = index === 0 ? '🥇' : index === 1 ? '🥈' : index === 2 ? '🥉' : `${index + 1}`
    ctx.fillText(medal, 50, y + 38)

    // 昵称
    ctx.font = 'bold 18px Arial'
    ctx.fillText(maskNickname(item.nickname), 100, y + 25)

    // 等级
    ctx.font = '14px Arial'
    ctx.fillStyle = '#666666'
    ctx.fillText(`LV.${item.level}`, 100, y + 48)

    y += 75
  })

  // 底部提示
  ctx.fillStyle = 'rgba(255, 255, 255, 0.9)'
  roundRect(ctx, 30, y + 20, 440, 100, 10)
  ctx.fill()

  ctx.fillStyle = '#333333'
  ctx.font = '14px Arial'
  ctx.textAlign = 'center'
  ctx.fillText('你的排名？扫码查看 →', 250, y + 50)

  // 小程序码（需要预先准备）
  // const qrCode = await loadImage('qrcode.png')
  // ctx.drawImage(qrCode, 175, y + 60, 150, 150)

  // 转换为Buffer
  return canvas.toBuffer('image/png')
}

function roundRect(ctx, x, y, width, height, radius) {
  ctx.beginPath()
  ctx.moveTo(x + radius, y)
  ctx.lineTo(x + width - radius, y)
  ctx.quadraticCurveTo(x + width, y, x + width, y + radius)
  ctx.lineTo(x + width, y + height - radius)
  ctx.quadraticCurveTo(x + width, y + height, x + width - radius, y + height)
  ctx.lineTo(x + radius, y + height)
  ctx.quadraticCurveTo(x, y + height, x, y + height - radius)
  ctx.lineTo(x, y + radius)
  ctx.quadraticCurveTo(x, y, x + radius, y)
  ctx.closePath()
}
```

### 简化方案：使用模板

```javascript
// 使用预设模板 + 数据替换
const templates = {
  ranking: {
    width: 500,
    height: 800,
    background: 'https://xxx.com/bg-ranking.png',
    elements: [
      { type: 'text', x: 250, y: 60, text: '🏆 千禧时光机 · 等级排行榜', align: 'center', size: 32, color: '#fff' },
      { type: 'list', x: 30, y: 140, width: 440, itemHeight: 75, dataField: 'rankings' }
    ]
  }
}

async function generateFromTemplate(templateName, data) {
  const template = templates[templateName]

  // 使用 canvas 绘制
  const canvas = createCanvas(template.width, template.height)
  const ctx = canvas.getContext('2d')

  // 绘制背景
  const bg = await loadImage(template.background)
  ctx.drawImage(bg, 0, 0)

  // 绘制元素
  for (const element of template.elements) {
    if (element.type === 'text') {
      ctx.font = `${element.size}px Arial`
      ctx.fillStyle = element.color
      ctx.textAlign = element.align
      ctx.fillText(element.text, element.x, element.y)
    } else if (element.type === 'list') {
      // 绘制列表
      let y = element.y
      data[element.dataField].forEach((item, index) => {
        // 绘制列表项
        drawListItem(ctx, item, index, element.x, y, element.width, element.itemHeight)
        y += element.itemHeight
      })
    }
  }

  return canvas.toBuffer('image/png')
}
```

### 小程序码生成

```javascript
// 生成小程序码
const cloud = require('wx-server-sdk')
cloud.init()

async function getQRCode() {
  const result = await cloud.openapi.wxacode.getUnlimited({
    scene: 'ranking',
    page: 'pages/index/index'
  })

  // 上传到云存储
  const upload = await cloud.uploadFile({
    cloudPath: `qrcodes/ranking_${Date.now()}.png`,
    fileContent: result.buffer
  })

  return upload.fileID
}
```

---

## 十、成本预估

### 云开发资源

| 资源 | 免费额度 | 预计用量 | 超额费用 |
|------|---------|---------|---------|
| **CDN流量** | 5GB/月 | ~2GB | 0元 |
| **数据库读** | 5万次/天 | ~1万次 | 0元 |
| **数据库写** | 3万次/天 | ~1000次 | 0元 |
| **云函数** | 4万次/月 | ~1000次 | 0元 |

**结论**：免费额度完全够用，无需额外成本。

---

## 十一、后续优化

| 优化项 | 说明 |
|--------|------|
| **移动端适配** | 响应式设计，手机也能查看 |
| **实时更新** | WebSocket推送实时数据 |
| **更多图表** | 更丰富的数据可视化 |
| **数据导出** | Excel/CSV导出功能 |
| **自定义仪表盘** | 用户自定义看板 |

---

> 后台管理系统是运营的眼睛，做好了能极大提升运营效率。
>
> © 2025 千禧时光机 · 后台管理系统
